# 01 - 起步：数据权限注解与接口

本章是数据权限组件实现的第一步，介绍核心注解 `@DataPermission`、规则接口 `DataPermissionRule` 和规则工厂 `DataPermissionRuleFactory` 的定义与实现。这些是整个数据权限体系的基石。

## @DataPermission 注解定义

`@DataPermission` 是数据权限的声明式入口，通过注解控制数据权限的行为。

```java
// 文件路径：yudao-framework/yudao-spring-boot-starter-biz-data-permission/src/main/java/cn/iocoder/yudao/framework/datapermission/core/annotation/DataPermission.java
package cn.iocoder.yudao.framework.datapermission.core.annotation;

import cn.iocoder.yudao.framework.datapermission.core.rule.DataPermissionRule;

import java.lang.annotation.*;

/**
 * 数据权限注解
 * 可声明在类或者方法上，标识使用的数据权限规则
 *
 * @author 芋道源码
 */
@Target({ElementType.TYPE, ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface DataPermission {

    /**
     * 当前类或方法是否开启数据权限
     * 即使不添加 @DataPermission 注解，默认是开启状态
     * 可通过设置 enable 为 false 禁用
     */
    boolean enable() default true;

    /**
     * 生效的数据权限规则数组，优先级高于 {@link #excludeRules()}
     */
    Class<? extends DataPermissionRule>[] includeRules() default {};

    /**
     * 排除的数据权限规则数组，优先级最低
     */
    Class<? extends DataPermissionRule>[] excludeRules() default {};

}
```

### 三个属性的作用

| 属性 | 类型 | 默认值 | 作用 | 优先级 |
|------|------|-------|------|--------|
| `enable` | `boolean` | `true` | 控制数据权限是否生效 | 最高 |
| `includeRules` | `Class<?>[]` | `{}` (空) | 指定生效的规则（白名单模式） | 中 |
| `excludeRules` | `Class<?>[]` | `{}` (空) | 指定排除的规则（黑名单模式） | 低 |

优先级规则：`enable = false` > `includeRules` > `excludeRules` > 默认全部规则

### 注解的生效范围

`@DataPermission` 可以标注在**类**或**方法**上，方法级别优先于类级别：

```java
// 类级别：作为默认值，对该类所有方法生效
@DataPermission
@RestController
@RequestMapping("/trade/order")
public class OrderController {

    // 方法级别：覆盖类级别的配置
    @DataPermission(enable = false)
    @GetMapping("/get")
    public CommonResult<OrderRespVO> getOrder(@RequestParam("id") Long id) {
        // 这个方法不进行数据权限过滤
    }

    // 未标注方法：使用类级别的 @DataPermission（enable = true）
    @GetMapping("/page")
    public CommonResult<PageResult<OrderRespVO>> getOrderPage(OrderPageReqVO reqVO) {
        // 这个方法进行数据权限过滤
    }
}
```

## DataPermissionRule 接口

`DataPermissionRule` 是规则体系的基石接口，定义了"哪些表需要过滤"以及"过滤条件是什么"。

```java
// 文件路径：yudao-framework/yudao-spring-boot-starter-biz-data-permission/src/main/java/cn/iocoder/yudao/framework/datapermission/core/rule/DataPermissionRule.java
package cn.iocoder.yudao.framework.datapermission.core.rule;

import com.baomidou.mybatisplus.core.metadata.TableInfoHelper;
import net.sf.jsqlparser.expression.Alias;
import net.sf.jsqlparser.expression.Expression;

import java.util.Set;

/**
 * 数据权限规则接口
 * 通过实现接口，自定义数据规则。例如说，
 *
 * @author 芋道源码
 */
public interface DataPermissionRule {

    /**
     * 返回需要生效的表名数组
     * 为什么需要该方法？Data Permission 数组基于 SQL 重写，通过 Where 返回只有权限的数据
     *
     * 如果需要基于实体名获得表名，可调用 {@link TableInfoHelper#getTableInfo(Class)} 获得
     *
     * @return 表名数组
     */
    Set<String> getTableNames();

    /**
     * 根据表名和别名，生成对应的 WHERE / OR 过滤条件
     *
     * @param tableName 表名
     * @param tableAlias 别名，可能为空
     * @return 过滤条件 Expression 表达式
     */
    Expression getExpression(String tableName, Alias tableAlias);

}
```

### 两个方法的设计意图

```
getTableNames()  →  "我负责哪些表？"
getExpression()  →  "这些表的过滤条件是什么？"
```

### 为什么返回 JSQLParser 的 Expression 而不是 SQL 字符串

`getExpression()` 返回的是 JSQLParser 库的 `Expression` 对象，而不是普通的 SQL 字符串。这个设计选择有深意：

| 方案 | 优点 | 缺点 |
|------|------|------|
| 返回 SQL 字符串 | 简单直观 | 需要自己处理 SQL 注入、表别名、AND/OR 拼接 |
| 返回 Expression 对象 | 类型安全、自动处理 SQL 语法、MyBatis-Plus 自动处理 AND 拼接 | 需要学习 JSQLParser API |

yudao-cloud 选择方案二，用类比来理解：返回 SQL 字符串就像用字符串拼 HTML（容易 XSS），返回 Expression 对象就像用 DOM API 构建 HTML（安全且结构化）。

## DataPermissionRuleFactory 接口

`DataPermissionRuleFactory` 是规则的容器和管理中心，负责根据当前上下文决定哪些规则应该生效。

```java
// 文件路径：yudao-framework/yudao-spring-boot-starter-biz-data-permission/src/main/java/cn/iocoder/yudao/framework/datapermission/core/rule/DataPermissionRuleFactory.java
package cn.iocoder.yudao.framework.datapermission.core.rule;

import java.util.List;

/**
 * {@link DataPermissionRule} 工厂接口
 * 作为 {@link DataPermissionRule} 的容器，提供管理能力
 *
 * @author 芋道源码
 */
public interface DataPermissionRuleFactory {

    /**
     * 获得所有数据权限规则数组
     *
     * @return 数据权限规则数组
     */
    List<DataPermissionRule> getDataPermissionRules();

    /**
     * 获得指定 Mapper 的数据权限规则数组
     *
     * @param mappedStatementId 指定 Mapper 的编号
     * @return 数据权限规则数组
     */
    List<DataPermissionRule> getDataPermissionRule(String mappedStatementId);

}
```

核心方法是 `getDataPermissionRule(String mappedStatementId)`，它根据当前的 `@DataPermission` 注解上下文来过滤规则。

## DataPermissionRuleFactoryImpl 实现

`DataPermissionRuleFactoryImpl` 是 `DataPermissionRuleFactory` 的默认实现，实现了根据注解上下文过滤规则的核心逻辑。

```java
// 文件路径：yudao-framework/yudao-spring-boot-starter-biz-data-permission/src/main/java/cn/iocoder/yudao/framework/datapermission/core/rule/DataPermissionRuleFactoryImpl.java
package cn.iocoder.yudao.framework.datapermission.core.rule;

import cn.hutool.core.collection.CollUtil;
import cn.hutool.core.util.ArrayUtil;
import cn.iocoder.yudao.framework.datapermission.core.annotation.DataPermission;
import cn.iocoder.yudao.framework.datapermission.core.aop.DataPermissionContextHolder;
import com.fhs.trans.service.impl.SimpleTransService;
import lombok.RequiredArgsConstructor;

import java.util.Collections;
import java.util.List;
import java.util.stream.Collectors;

/**
 * 默认的 DataPermissionRuleFactoryImpl 实现类
 * 支持通过 {@link DataPermissionContextHolder} 过滤数据权限
 *
 * @author 芋道源码
 */
@RequiredArgsConstructor
public class DataPermissionRuleFactoryImpl implements DataPermissionRuleFactory {

    /**
     * 数据权限规则数组
     */
    private final List<DataPermissionRule> rules;

    @Override
    public List<DataPermissionRule> getDataPermissionRules() {
        return rules;
    }

    @Override // mappedStatementId 参数，暂时没有用。以后，可以基于 mappedStatementId + DataPermission 进行缓存
    public List<DataPermissionRule> getDataPermissionRule(String mappedStatementId) {
        // 1.1 无数据权限
        if (CollUtil.isEmpty(rules)) {
            return Collections.emptyList();
        }
        // 1.2 未配置，则默认开启
        DataPermission dataPermission = DataPermissionContextHolder.get();
        if (dataPermission == null) {
            return rules;
        }
        // 1.3 已配置，但禁用
        if (!dataPermission.enable()) {
            return Collections.emptyList();
        }
        // 1.4 特殊：数据翻译时，强制忽略数据权限 https://github.com/YunaiV/ruoyi-vue-pro/issues/1007
        if (isTranslateCall()) {
            return Collections.emptyList();
        }

        // 2.1 情况一：已配置，只选择部分规则
        if (ArrayUtil.isNotEmpty(dataPermission.includeRules())) {
            return rules.stream().filter(rule -> ArrayUtil.contains(dataPermission.includeRules(), rule.getClass()))
                    .collect(Collectors.toList()); // 一般规则不会太多，所以不采用 HashSet 查询
        }
        // 2.2 已配置，只排除部分规则
        if (ArrayUtil.isNotEmpty(dataPermission.excludeRules())) {
            return rules.stream().filter(rule -> !ArrayUtil.contains(dataPermission.excludeRules(), rule.getClass()))
                    .collect(Collectors.toList()); // 一般规则不会太多，所以不采用 HashSet 查询
        }
        // 2.3 已配置，全部规则
        return rules;
    }

    /**
     * 判断是否为数据翻译 {@link com.fhs.core.trans.anno.Trans} 的调用
     *
     * 目前暂时只有这个办法，已经和 easy-trans 做过沟通
     *
     * @return 是否
     */
    private boolean isTranslateCall() {
        StackTraceElement[] stack = Thread.currentThread().getStackTrace();
        for (StackTraceElement e : stack) {
            if (SimpleTransService.class.getName().equals(e.getClassName())) {
                return true;
            }
        }
        return false;
    }

}
```

### 过滤逻辑决策树

```
getDataPermissionRule() 被调用
    │
    ▼
规则列表为空？ ──是──▶ 返回空列表
    │否
    ▼
有 @DataPermission 注解？ ──否──▶ 返回所有规则（默认开启）
    │是
    ▼
enable = false？ ──是──▶ 返回空列表（禁用数据权限）
    │否
    ▼
是数据翻译调用？ ──是──▶ 返回空列表（避免干扰）
    │否
    ▼
配置了 includeRules？ ──是──▶ 返回 includeRules 中指定的规则
    │否
    ▼
配置了 excludeRules？ ──是──▶ 返回排除 excludeRules 后的规则
    │否
    ▼
返回所有规则
```

### 优先级：includeRules > excludeRules

`includeRules` 的优先级高于 `excludeRules`，这是一个重要的设计决策：

```java
// 场景：系统有 3 个规则：DeptRule、RegionRule、CustomerLevelRule

// 只使用 DeptRule
@DataPermission(includeRules = DeptDataPermissionRule.class)
public PageResult<OrderDO> getOrderPage() { ... }

// 排除 RegionRule（使用 DeptRule + CustomerLevelRule）
@DataPermission(excludeRules = RegionDataPermissionRule.class)
public PageResult<OrderDO> getOrderPage() { ... }

// 注意：如果同时配置了 includeRules 和 excludeRules
// 只有 includeRules 生效，excludeRules 被忽略
@DataPermission(includeRules = DeptDataPermissionRule.class,
               excludeRules = RegionDataPermissionRule.class)
// 实际效果：只使用 DeptRule
```

### isTranslateCall()：数据翻译的特殊处理

`isTranslateCall()` 方法通过检查调用栈来判断是否是数据翻译（easy-trans）的调用。这是因为数据翻译时需要查询原始数据，不应该被数据权限过滤。

```java
private boolean isTranslateCall() {
    StackTraceElement[] stack = Thread.currentThread().getStackTrace();
    for (StackTraceElement e : stack) {
        if (SimpleTransService.class.getName().equals(e.getClassName())) {
            return true;
        }
    }
    return false;
}
```

这种"黑科技"的潜在风险：
- 依赖于 easy-trans 的实现细节，如果 easy-trans 修改了类名，这里会失效
- 性能开销：每次调用都要遍历调用栈

但目前没有更好的方案，因为 easy-trans 没有提供专门的扩展点。

## 思考题

1. `@DataPermission` 默认是开启的（`enable = true`），为什么选择"默认开启"而不是"默认关闭"？这两种策略各有什么优缺点？

2. `DataPermissionRuleFactoryImpl` 中的 `isTranslateCall()` 方法通过检查调用栈来判断是否是数据翻译调用。这种"黑科技"有什么潜在风险？有没有更好的方案？

3. 如果你需要实现一个新的数据权限规则，比如"基于客户等级的数据权限"（VIP 客户只能被高级销售看到），你会如何设计？需要实现哪些方法？

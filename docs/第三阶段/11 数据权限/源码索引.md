# 源码索引：数据权限

## 源码文件列表

### 框架层（yudao-spring-boot-starter-biz-data-permission）

#### 注解层（annotation）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ DataPermission | `framework/datapermission/core/annotation/DataPermission.java` | 数据权限注解，控制 enable / includeRules / excludeRules |

#### AOP 层（aop）

| 文件 | 路径 | 说明 |
|------|------|------|
| DataPermissionAnnotationAdvisor | `framework/datapermission/core/aop/DataPermissionAnnotationAdvisor.java` | AOP Advisor，定义切点（类或方法上有 @DataPermission） |
| ★ DataPermissionAnnotationInterceptor | `framework/datapermission/core/aop/DataPermissionAnnotationInterceptor.java` | AOP 拦截器，将注解压入/弹出上下文栈，含注解缓存 |
| ★ DataPermissionContextHolder | `framework/datapermission/core/aop/DataPermissionContextHolder.java` | 基于 TransmittableThreadLocal 的上下文存储，LinkedList 栈结构 |

#### SQL 改写层（db）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ DataPermissionRuleHandler | `framework/datapermission/core/db/DataPermissionRuleHandler.java` | MyBatis-Plus MultiDataPermissionHandler 实现，SQL 改写核心 |

#### 规则层（rule）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ DataPermissionRule | `framework/datapermission/core/rule/DataPermissionRule.java` | 规则接口，定义 getTableNames() 和 getExpression() |
| DataPermissionRuleFactory | `framework/datapermission/core/rule/DataPermissionRuleFactory.java` | 规则工厂接口 |
| ★ DataPermissionRuleFactoryImpl | `framework/datapermission/core/rule/DataPermissionRuleFactoryImpl.java` | 工厂实现，根据 @DataPermission 注解上下文过滤规则 |

#### 部门规则层（rule/dept）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ DeptDataPermissionRule | `framework/datapermission/core/rule/dept/DeptDataPermissionRule.java` | 基于部门的规则实现，支持 dept_id 和 user_id 过滤 |
| DeptDataPermissionRuleCustomizer | `framework/datapermission/core/rule/dept/DeptDataPermissionRuleCustomizer.java` | 函数式接口，用于配置表的字段映射 |

#### 工具层（util）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ DataPermissionUtils | `framework/datapermission/core/util/DataPermissionUtils.java` | 编程式禁用数据权限（executeIgnore / addDisableDataPermission） |

#### RPC 传播层（rpc）

| 文件 | 路径 | 说明 |
|------|------|------|
| DataPermissionRequestInterceptor | `framework/datapermission/core/rpc/DataPermissionRequestInterceptor.java` | Feign 拦截器，发送端传播 enable=false 到 Header |
| DataPermissionRpcWebFilter | `framework/datapermission/core/rpc/DataPermissionRpcWebFilter.java` | Web Filter，接收端从 Header 恢复数据权限上下文 |

#### 自动配置层（config）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ YudaoDataPermissionAutoConfiguration | `framework/datapermission/config/YudaoDataPermissionAutoConfiguration.java` | 核心自动配置：规则工厂、Handler、AOP Advisor |
| ★ YudaoDeptDataPermissionAutoConfiguration | `framework/datapermission/config/YudaoDeptDataPermissionAutoConfiguration.java` | 部门规则自动配置，条件：有 DeptDataPermissionRuleCustomizer Bean |
| YudaoDataPermissionRpcAutoConfiguration | `framework/datapermission/config/YudaoDataPermissionRpcAutoConfiguration.java` | RPC 传播自动配置，条件：有 feign.RequestInterceptor |

### 公共层（yudao-common）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ PermissionCommonApi | `framework/common/biz/system/permission/PermissionCommonApi.java` | 权限 RPC 接口，定义 getDeptDataPermission() |
| ★ DeptDataPermissionRespDTO | `framework/common/biz/system/permission/dto/DeptDataPermissionRespDTO.java` | 数据权限传输对象（all, self, deptIds） |
| ★ DataScopeEnum | `module/system/enums/permission/DataScopeEnum.java` | 五级数据范围枚举（ALL, DEPT_CUSTOM, DEPT_ONLY, DEPT_AND_CHILD, SELF） |
| UserTypeEnum | `framework/common/enums/UserTypeEnum.java` | 用户类型枚举（ADMIN, MEMBER） |

### 业务层（yudao-module-system）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ PermissionServiceImpl | `module/system/service/permission/PermissionServiceImpl.java` | getDeptDataPermission() 计算数据权限，含 @DataPermission(enable=false) |
| RoleDO | `module/system/dal/dataobject/permission/RoleDO.java` | 角色实体，包含 dataScope 和 dataScopeDeptIds 字段 |
| ★ DataPermissionConfiguration | `module/system/framework/datapermission/config/DataPermissionConfiguration.java` | system 模块的规则配置：addDeptColumn / addUserColumn |
| UserProfileController | `module/system/controller/admin/user/UserProfileController.java` | 使用 @DataPermission(enable=false) 的示例 |

### 测试层

| 文件 | 路径 | 说明 |
|------|------|------|
| DataPermissionRuleHandlerTest | `test/core/db/DataPermissionRuleHandlerTest.java` | SQL 改写处理器测试，覆盖单表/多表/JOIN/子查询等场景 |
| DataPermissionAnnotationInterceptorTest | `test/core/aop/DataPermissionAnnotationInterceptorTest.java` | AOP 拦截器测试 |
| DataPermissionContextHolderTest | `test/core/aop/DataPermissionContextHolderTest.java` | 上下文管理器测试 |
| DataPermissionRuleFactoryImplTest | `test/core/rule/DataPermissionRuleFactoryImplTest.java` | 规则工厂过滤逻辑测试 |
| DeptDataPermissionRuleTest | `test/core/rule/dept/DeptDataPermissionRuleTest.java` | 部门规则 SQL 生成测试 |
| DataPermissionUtilsTest | `test/core/util/DataPermissionUtilsTest.java` | 工具类测试 |

## 业务层使用示例

### 声明式使用（注解）

```java
// 1. 默认开启数据权限（不加注解或 @DataPermission）
@GetMapping("/page")
public CommonResult<PageResult<OrderRespVO>> getOrderPage(OrderPageReqVO reqVO) { ... }

// 2. 禁用数据权限
@DataPermission(enable = false)
@GetMapping("/get")
public CommonResult<OrderRespVO> getOrder(@RequestParam("id") Long id) { ... }

// 3. 只使用特定规则
@DataPermission(includeRules = DeptDataPermissionRule.class)
public PageResult<OrderDO> getOrderPage(OrderPageReqVO reqVO) { ... }

// 4. 排除特定规则
@DataPermission(excludeRules = RegionDataPermissionRule.class)
public PageResult<OrderDO> getOrderPage(OrderPageReqVO reqVO) { ... }
```

### 编程式使用（工具类）

```java
// 1. Lambda 中禁用数据权限
DataPermissionUtils.executeIgnore(() -> {
    List<OrderDO> orders = orderMapper.selectList(...);
});

// 2. 带返回值
OrderDO order = DataPermissionUtils.executeIgnore(() -> {
    return orderMapper.selectById(orderId);
});

// 3. 手动控制上下文
DataPermissionUtils.addDisableDataPermission();
try {
    // 业务逻辑
} finally {
    DataPermissionUtils.removeDataPermission();
}
```

### 配置表字段映射

```java
@Configuration(proxyBeanMethods = false)
public class TradeDataPermissionConfiguration {

    @Bean
    public DeptDataPermissionRuleCustomizer tradeDeptDataPermissionRuleCustomizer() {
        return rule -> {
            // 部门过滤：订单表的 dept_id 字段
            rule.addDeptColumn(TradeOrderDO.class);
            // 用户过滤：订单表的 user_id 字段
            rule.addUserColumn(TradeOrderDO.class);
            // 部门过滤：客户表的 dept_id 字段
            rule.addDeptColumn(TradeCustomerDO.class);
            // 用户过滤：客户表的 creator 字段（自定义列名）
            rule.addUserColumn(TradeCustomerDO.class, "creator");
        };
    }
}
```

## 章节 ↔ 源码对照表

### 功能设计

| 章节 | 核心源码 |
|------|---------|
| 功能设计 01-总览 | DataPermission, DataPermissionRuleHandler, DataPermissionRuleFactory, DeptDataPermissionRule |
| 功能设计 02-问题域 | MultiDataPermissionHandler (MyBatis-Plus) |
| 功能设计 03-规则体系 | ★ DataPermissionRule, ★ DataPermissionRuleFactoryImpl, ★ DataPermissionRuleHandler, DeptDataPermissionRule |
| 功能设计 04-注解驱动 | ★ DataPermission, DataPermissionAnnotationAdvisor, ★ DataPermissionAnnotationInterceptor, ★ DataPermissionContextHolder, DataPermissionUtils |
| 功能设计 05-跨服务传播 | DataPermissionRequestInterceptor, DataPermissionRpcWebFilter, YudaoDataPermissionRpcAutoConfiguration |
| 功能设计 06-精简汇总 | 全部核心类 |

### 功能实现

| 章节 | 核心源码 |
|------|---------|
| 功能实现 01-注解与接口 | ★ DataPermission, ★ DataPermissionRule, DataPermissionRuleFactory, ★ DataPermissionRuleFactoryImpl |
| 功能实现 02-SQL改写 | ★ DataPermissionRuleHandler, ★ YudaoDataPermissionAutoConfiguration, MyBatisUtils |
| 功能实现 03-部门规则 | ★ DeptDataPermissionRule, DeptDataPermissionRuleCustomizer, DeptDataPermissionRespDTO, ★ YudaoDeptDataPermissionAutoConfiguration, DataPermissionConfiguration |
| 功能实现 04-业务集成 | UserProfileController, ★ PermissionServiceImpl, ★ DataPermissionUtils, DataPermissionContextHolder |
| 功能实现 05-测试与索引 | DataPermissionRuleHandlerTest, DataPermissionAnnotationInterceptorTest, DataPermissionContextHolderTest, DataPermissionRuleFactoryImplTest, DeptDataPermissionRuleTest, DataPermissionUtilsTest |

### 深度学习

| 章节 | 关联源码 |
|------|---------|
| 深度学习 01-设计模式提炼 | ★ DataPermissionRule（策略模式）, ★ DataPermissionRuleHandler（责任链）, ★ DataPermission（注解驱动）, DataPermissionRuleFactory（工厂模式） |

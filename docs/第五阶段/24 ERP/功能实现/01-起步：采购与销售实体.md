# 01-起步：采购与销售实体

本章实现 ERP 模块的采购和销售核心实体，让读者理解主子表设计和审核状态机的代码实现。

> **前置阅读**：本章对应功能设计中的「02-核心设计：进销存与财务」中主子表设计和审核状态机部分。

## 模块结构

ERP 模块位于 `yudao-module-erp/yudao-module-erp-server`：

```
yudao-module-erp-server/src/main/java/cn/iocoder/yudao/module/erp/
├── controller/admin/          ← 管理后台 API（/admin-api/erp）
│   ├── product/               ← 产品管理
│   ├── purchase/              ← 采购管理（供应商/采购订单/采购入库/采购退货）
│   ├── sale/                  ← 销售管理（客户/销售订单/销售出库/销售退货）
│   ├── stock/                 ← 库存管理（库存/入库/出库/盘点/调拨/仓库）
│   ├── finance/               ← 财务管理（账户/收款单/付款单）
│   └── statistics/            ← 统计分析
├── service/                   ← 业务逻辑层
├── dal/
│   ├── dataobject/            ← DO 实体
│   └── mysql/                 ← Mapper 接口
├── dal/redis/                 ← Redis（订单号生成）
└── enums/                     ← 枚举定义
```

## 采购订单主表：ErpPurchaseOrderDO

源码位置：`yudao-module-erp-server/.../dal/dataobject/purchase/ErpPurchaseOrderDO.java`

```java
@TableName(value = "erp_purchase_order")
@KeySequence("erp_purchase_order_seq")
@Data
@EqualsAndHashCode(callSuper = true)
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ErpPurchaseOrderDO extends BaseDO {

    @TableId
    private Long id;
    private String no;                // 采购订单号
    private Integer status;           // 采购状态，枚举 ErpAuditStatus
    private Long supplierId;          // 供应商编号，关联 ErpSupplierDO
    private Long accountId;           // 结算账户编号，关联 ErpAccountDO
    private LocalDateTime orderTime;  // 下单时间

    // ========== 汇总金额 ==========
    private BigDecimal totalCount;        // 合计数量
    private BigDecimal totalPrice;        // 最终合计价格
    private BigDecimal totalProductPrice; // 合计产品价格
    private BigDecimal totalTaxPrice;     // 合计税额
    private BigDecimal discountPercent;   // 优惠率（百分比）
    private BigDecimal discountPrice;     // 优惠金额
    private BigDecimal depositPrice;      // 定金金额

    private String fileUrl;           // 附件地址
    private String remark;            // 备注

    // ========== 执行进度 ==========
    private BigDecimal inCount;       // 已入库数量
    private BigDecimal returnCount;   // 已退货数量
}
```

**设计要点**：

- `inCount` 和 `returnCount` 是冗余字段，可以从入库单/退货单聚合计算。但冗余存储避免了每次查询都 JOIN 多张表，是典型的**读优化**设计
- 金额字段使用 `BigDecimal` 而非 `Integer`（分），因为 ERP 场景金额通常以"元"为单位，需要支持小数
- `status` 使用统一的 `ErpAuditStatus` 枚举，采购和销售共享同一套审核状态

## 采购订单明细：ErpPurchaseOrderItemDO

源码位置：`yudao-module-erp-server/.../dal/dataobject/purchase/ErpPurchaseOrderItemDO.java`

```java
@TableName("erp_purchase_order_items")
@KeySequence("erp_purchase_order_items_seq")
@Data
@EqualsAndHashCode(callSuper = true)
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ErpPurchaseOrderItemDO extends BaseDO {

    @TableId
    private Long id;
    private Long orderId;             // 采购订单编号，关联 ErpPurchaseOrderDO
    private Long productId;           // 产品编号，关联 ErpProductDO
    private Long productUnitId;       // 产品单位（冗余）

    private BigDecimal productPrice;  // 产品单价，单位：元
    private BigDecimal count;         // 数量
    private BigDecimal totalPrice;    // 总价 = productPrice * count
    private BigDecimal taxPercent;    // 税率（百分比）
    private BigDecimal taxPrice;      // 税额 = totalPrice * taxPercent / 100
    private String remark;            // 备注

    // ========== 执行进度 ==========
    private BigDecimal inCount;       // 已入库数量
    private BigDecimal returnCount;   // 已退货数量
}
```

**金额计算链路**：

```
明细层：
  totalPrice = productPrice * count
  taxPrice = totalPrice * taxPercent / 100

订单头层：
  totalCount = SUM(item.count)
  totalProductPrice = SUM(item.totalPrice)
  totalTaxPrice = SUM(item.taxPrice)
  discountPrice = (totalProductPrice + totalTaxPrice) * discountPercent / 100
  totalPrice = totalProductPrice + totalTaxPrice - discountPrice
```

## 销售订单：ErpSaleOrderDO

源码位置：`yudao-module-erp-server/.../dal/dataobject/sale/ErpSaleOrderDO.java`

```java
@TableName(value = "erp_sale_order")
@KeySequence("erp_sale_order_seq")
@Data
@EqualsAndHashCode(callSuper = true)
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ErpSaleOrderDO extends BaseDO {

    @TableId
    private Long id;
    private String no;                // 销售订单号
    private Integer status;           // 销售状态，枚举 ErpAuditStatus
    private Long customerId;          // 客户编号，关联 ErpCustomerDO
    private Long accountId;           // 结算账户编号，关联 ErpAccountDO
    private Long saleUserId;          // 销售员编号
    private LocalDateTime orderTime;  // 下单时间

    // ========== 汇总金额 ==========
    private BigDecimal totalCount;        // 合计数量
    private BigDecimal totalPrice;        // 最终合计价格
    private BigDecimal totalProductPrice; // 合计产品价格
    private BigDecimal totalTaxPrice;     // 合计税额
    private BigDecimal discountPercent;   // 优惠率
    private BigDecimal discountPrice;     // 优惠金额
    private BigDecimal depositPrice;      // 定金金额

    private String fileUrl;           // 附件地址
    private String remark;            // 备注

    // ========== 执行进度 ==========
    private BigDecimal outCount;      // 已出库数量
    private BigDecimal returnCount;   // 已退货数量
}
```

**采购与销售的对称设计**：

```
采购订单                          销售订单
  │                                │
  ├── 供应商 (supplierId)          ├── 客户 (customerId)
  ├── 采购明细 (OrderItemDO)       ├── 销售明细 (OrderItemDO)
  ├── 入库跟踪 (inCount)           ├── 出库跟踪 (outCount)
  └── 退货跟踪 (returnCount)       └── 退货跟踪 (returnCount)
```

对称设计让两套流程的代码结构一致，降低理解和维护成本。

## 审核状态机：ErpAuditStatus

源码位置：`yudao-module-erp-api/.../enums/ErpAuditStatus.java`

采购订单和销售订单共享同一套审核状态枚举：

```java
public enum ErpAuditStatus {
    DRAFT(0, "草稿"),
    PROCESS(10, "审核中"),
    APPROVE(20, "审核通过"),
    REJECT(30, "审核驳回");
    // 注意：COMPLETED(40) 在某些版本中存在
}
```

状态流转：

```
                ┌──────────────────────────────────────┐
                │                                      │
                ▼                                      │
  ┌──────────┐  提交  ┌──────────┐  通过  ┌──────────┐ │
  │  草稿(0)  │──────▶│审核中(10) │──────▶│审核通过(20)│ │
  └──────────┘       └──────────┘       └──────────┘ │
       │                  │                   │        │
       │ 删除             │ 驳回              │ 完成    │
       ▼                  ▼                   ▼        │
    [已删除]         ┌──────────┐       ┌──────────┐   │
                     │审核驳回(30)│       │已完成(40) │   │
                     └──────────┘       └──────────┘   │
                          │                             │
                          │ 修改后重新提交               │
                          └─────────────────────────────┘
```

## 采购订单创建流程

源码位置：`yudao-module-erp-server/.../service/purchase/ErpPurchaseOrderServiceImpl.java`

```java
@Override
@Transactional(rollbackFor = Exception.class)
public Long createPurchaseOrder(ErpPurchaseOrderSaveReqVO createReqVO) {
    // 1.1 校验订单项的有效性
    List<ErpPurchaseOrderItemDO> purchaseOrderItems = validatePurchaseOrderItems(createReqVO.getItems());
    // 1.2 校验供应商
    supplierService.validateSupplier(createReqVO.getSupplierId());
    // 1.3 校验结算账户
    if (createReqVO.getAccountId() != null) {
        accountService.validateAccount(createReqVO.getAccountId());
    }
    // 1.4 生成订单号，并校验唯一性
    String no = noRedisDAO.generate(ErpNoRedisDAO.PURCHASE_ORDER_NO_PREFIX);
    if (purchaseOrderMapper.selectByNo(no) != null) {
        throw exception(PURCHASE_ORDER_NO_EXISTS);
    }

    // 2.1 插入订单
    ErpPurchaseOrderDO purchaseOrder = BeanUtils.toBean(createReqVO, ErpPurchaseOrderDO.class, in -> in
            .setNo(no).setStatus(ErpAuditStatus.PROCESS.getStatus()));
    calculateTotalPrice(purchaseOrder, purchaseOrderItems);
    purchaseOrderMapper.insert(purchaseOrder);
    // 2.2 插入订单项
    purchaseOrderItems.forEach(o -> o.setOrderId(purchaseOrder.getId()));
    purchaseOrderItemMapper.insertBatch(purchaseOrderItems);
    return purchaseOrder.getId();
}
```

**设计要点**：

- 订单号通过 `ErpNoRedisDAO` 生成，保证唯一性
- 创建时默认状态为 `PROCESS`（审核中）
- 使用事务保证订单主表和明细表的联合写入

### 订单项校验

```java
private List<ErpPurchaseOrderItemDO> validatePurchaseOrderItems(List<ErpPurchaseOrderSaveReqVO.Item> list) {
    // 1. 校验产品存在
    List<ErpProductDO> productList = productService.validProductList(
            convertSet(list, ErpPurchaseOrderSaveReqVO.Item::getProductId));
    Map<Long, ErpProductDO> productMap = convertMap(productList, ErpProductDO::getId);
    // 2. 转化为 ErpPurchaseOrderItemDO 列表
    return convertList(list, o -> BeanUtils.toBean(o, ErpPurchaseOrderItemDO.class, item -> {
        item.setProductUnitId(productMap.get(item.getProductId()).getUnitId());
        item.setTotalPrice(MoneyUtils.priceMultiply(item.getProductPrice(), item.getCount()));
        if (item.getTotalPrice() == null) {
            return;
        }
        if (item.getTaxPercent() != null) {
            item.setTaxPrice(MoneyUtils.priceMultiplyPercent(item.getTotalPrice(), item.getTaxPercent()));
        }
    }));
}
```

### 金额计算

```java
private void calculateTotalPrice(ErpPurchaseOrderDO purchaseOrder, List<ErpPurchaseOrderItemDO> purchaseOrderItems) {
    purchaseOrder.setTotalCount(getSumValue(purchaseOrderItems, ErpPurchaseOrderItemDO::getCount, BigDecimal::add));
    purchaseOrder.setTotalProductPrice(getSumValue(purchaseOrderItems, ErpPurchaseOrderItemDO::getTotalPrice, BigDecimal::add, BigDecimal.ZERO));
    purchaseOrder.setTotalTaxPrice(getSumValue(purchaseOrderItems, ErpPurchaseOrderItemDO::getTaxPrice, BigDecimal::add, BigDecimal.ZERO));
    purchaseOrder.setTotalPrice(purchaseOrder.getTotalProductPrice().add(purchaseOrder.getTotalTaxPrice()));
    // 计算优惠价格
    if (purchaseOrder.getDiscountPercent() == null) {
        purchaseOrder.setDiscountPercent(BigDecimal.ZERO);
    }
    purchaseOrder.setDiscountPrice(MoneyUtils.priceMultiplyPercent(purchaseOrder.getTotalPrice(), purchaseOrder.getDiscountPercent()));
    purchaseOrder.setTotalPrice(purchaseOrder.getTotalPrice().subtract(purchaseOrder.getDiscountPrice()));
}
```

## 审核状态变更

```java
@Override
@Transactional(rollbackFor = Exception.class)
public void updatePurchaseOrderStatus(Long id, Integer status) {
    boolean approve = ErpAuditStatus.APPROVE.getStatus().equals(status);
    // 1.1 校验存在
    ErpPurchaseOrderDO purchaseOrder = validatePurchaseOrderExists(id);
    // 1.2 校验状态
    if (purchaseOrder.getStatus().equals(status)) {
        throw exception(approve ? PURCHASE_ORDER_APPROVE_FAIL : PURCHASE_ORDER_PROCESS_FAIL);
    }
    // 1.3 存在采购入库单，无法反审核
    if (!approve && purchaseOrder.getInCount().compareTo(BigDecimal.ZERO) > 0) {
        throw exception(PURCHASE_ORDER_PROCESS_FAIL_EXISTS_IN);
    }
    // 1.4 存在采购退货单，无法反审核
    if (!approve && purchaseOrder.getReturnCount().compareTo(BigDecimal.ZERO) > 0) {
        throw exception(PURCHASE_ORDER_PROCESS_FAIL_EXISTS_RETURN);
    }

    // 2. 更新状态（乐观锁）
    int updateCount = purchaseOrderMapper.updateByIdAndStatus(id, purchaseOrder.getStatus(),
            new ErpPurchaseOrderDO().setStatus(status));
    if (updateCount == 0) {
        throw exception(approve ? PURCHASE_ORDER_APPROVE_FAIL : PURCHASE_ORDER_PROCESS_FAIL);
    }
}
```

**设计要点**：

- 反审核时检查是否已存在入库单或退货单，防止数据不一致
- 使用 `updateByIdAndStatus` 实现乐观锁，防止并发状态变更

## 订单明细更新

更新订单时，明细的处理采用**对比差异**的方式：

```java
private void updatePurchaseOrderItemList(Long id, List<ErpPurchaseOrderItemDO> newList) {
    // 第一步，对比新老数据，获得添加、修改、删除的列表
    List<ErpPurchaseOrderItemDO> oldList = purchaseOrderItemMapper.selectListByOrderId(id);
    List<List<ErpPurchaseOrderItemDO>> diffList = diffList(oldList, newList,
            (oldVal, newVal) -> oldVal.getId().equals(newVal.getId()));

    // 第二步，批量添加、修改、删除
    if (CollUtil.isNotEmpty(diffList.get(0))) {
        diffList.get(0).forEach(o -> o.setOrderId(id));
        purchaseOrderItemMapper.insertBatch(diffList.get(0));
    }
    if (CollUtil.isNotEmpty(diffList.get(1))) {
        purchaseOrderItemMapper.updateBatch(diffList.get(1));
    }
    if (CollUtil.isNotEmpty(diffList.get(2))) {
        purchaseOrderItemMapper.deleteByIds(convertList(diffList.get(2), ErpPurchaseOrderItemDO::getId));
    }
}
```

这是一个通用的"主子表更新"模式：先查出旧明细，与新明细对比 ID，分别执行新增、修改、删除。

## 执行进度跟踪

采购入库/退货时，需要更新订单的执行进度：

```java
@Override
public void updatePurchaseOrderInCount(Long id, Map<Long, BigDecimal> inCountMap) {
    List<ErpPurchaseOrderItemDO> orderItems = purchaseOrderItemMapper.selectListByOrderId(id);
    // 1. 更新每个采购订单项
    orderItems.forEach(item -> {
        BigDecimal inCount = inCountMap.getOrDefault(item.getId(), BigDecimal.ZERO);
        if (item.getInCount().equals(inCount)) {
            return;
        }
        if (inCount.compareTo(item.getCount()) > 0) {
            throw exception(PURCHASE_ORDER_ITEM_IN_FAIL_PRODUCT_EXCEED,
                    productService.getProduct(item.getProductId()).getName(), item.getCount());
        }
        purchaseOrderItemMapper.updateById(new ErpPurchaseOrderItemDO().setId(item.getId()).setInCount(inCount));
    });
    // 2. 更新采购订单
    BigDecimal totalInCount = getSumValue(inCountMap.values(), value -> value, BigDecimal::add, BigDecimal.ZERO);
    purchaseOrderMapper.updateById(new ErpPurchaseOrderDO().setId(id).setInCount(totalInCount));
}
```

入库数量不能超过订单数量，这是一个业务约束。

## 基础数据实体

### 供应商：ErpSupplierDO

源码位置：`yudao-module-erp-server/.../dal/dataobject/purchase/ErpSupplierDO.java`

```java
@TableName("erp_supplier")
@Data
public class ErpSupplierDO extends BaseDO {
    private Long id;
    private String name;              // 供应商名称
    private Long contactId;           // 联系人
    private String mobile;            // 手机
    private String telephone;         // 电话
    private String email;             // 邮箱
    private Integer areaId;           // 所在地
    private String detailAddress;     // 详细地址
    private Integer taxPercent;       // 税率
    private Long accountId;           // 结算账户
    private BigDecimal openingBalance;// 期初余额
    private String remark;            // 备注
}
```

### 客户：ErpCustomerDO

源码位置：`yudao-module-erp-server/.../dal/dataobject/sale/ErpCustomerDO.java`

与供应商结构对称，用于销售管理。

### 产品：ErpProductDO

源码位置：`yudao-module-erp-server/.../dal/dataobject/product/ErpProductDO.java`

```java
@TableName("erp_product")
@Data
public class ErpProductDO extends BaseDO {
    private Long id;
    private String name;              // 产品名称
    private Long categoryId;          // 产品分类
    private Long unitId;              // 计量单位
    private BigDecimal price;         // 标准价格
    private BigDecimal costPrice;     // 成本价格
    private String barCode;           // 条码
    private String remark;            // 备注
}
```

## 小结

| 组件 | 职责 | 关键设计 |
|------|------|---------|
| ErpPurchaseOrderDO | 采购订单主表 | 冗余 inCount/returnCount 跟踪执行进度 |
| ErpPurchaseOrderItemDO | 采购订单明细 | BigDecimal 金额 + 税率计算 |
| ErpSaleOrderDO | 销售订单主表 | 与采购订单对称设计 |
| ErpAuditStatus | 审核状态枚举 | 采购/销售共享同一套状态机 |
| ErpSupplierDO | 供应商 | 基础数据实体 |
| ErpProductDO | 产品 | ERP 产品（区别于电商商品） |

> **下一章**：「02-库存与财务实现」将介绍库存变动机制和财务收付款的完整实现。

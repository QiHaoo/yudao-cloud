# 生产工单与报工实现

## 1. 生产工单管理

### 1.1 工单数据模型

MES 生产工单的核心数据对象 `MesProWorkOrderDO` 定义了工单的完整信息：

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/dal/dataobject/pro/workorder/MesProWorkOrderDO.java

@TableName("mes_pro_work_order")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesProWorkOrderDO extends BaseDO {

    /**
     * 父工单编号，空值
     */
    public static final Long PARENT_ID_NULL = 0L;

    @TableId
    private Long id;
    
    /**
     * 工单编码
     */
    private String code;
    
    /**
     * 工单名称
     */
    private String name;
    
    /**
     * 工单类型
     * 字典 MES_PRO_WORK_ORDER_TYPE，枚举 MesProWorkOrderTypeEnum
     */
    private Integer type;
    
    /**
     * 来源类型
     * 字典 MES_PRO_WORK_ORDER_SOURCE_TYPE，枚举 MesProWorkOrderSourceTypeEnum
     */
    private Integer orderSourceType;
    
    /**
     * 来源单据编号
     */
    private String orderSourceCode;
    
    /**
     * 产品编号，关联 MesMdItemDO
     */
    private Long productId;
    
    /**
     * 生产数量
     */
    private BigDecimal quantity;
    
    /**
     * 已生产数量
     */
    private BigDecimal quantityProduced;
    
    /**
     * 调整数量
     */
    private BigDecimal quantityChanged;
    
    /**
     * 已排产数量
     */
    private BigDecimal quantityScheduled;
    
    /**
     * 客户编号，关联 MesMdClientDO
     */
    private Long clientId;
    
    /**
     * 供应商编号，关联 MesMdVendorDO
     */
    private Long vendorId;
    
    /**
     * 批次号
     */
    private String batchCode;
    
    /**
     * 需求日期
     */
    private LocalDateTime requestDate;
    
    /**
     * 父工单编号，关联 MesProWorkOrderDO
     */
    private Long parentId;
    
    /**
     * 完成时间
     */
    private LocalDateTime finishDate;
    
    /**
     * 取消时间
     */
    private LocalDateTime cancelDate;
    
    /**
     * 工单状态
     * 字典 MES_PRO_WORK_ORDER_STATUS，枚举 MesProWorkOrderStatusEnum
     */
    private Integer status;
    
    /**
     * 备注
     */
    private String remark;
}
```

**关键设计说明**：
- `parentId`：支持工单拆分，父工单可拆分为多个子工单
- `quantityProduced`：实时跟踪已生产数量
- `quantityScheduled`：跟踪已排产数量，防止过度排产
- `orderSourceType` + `orderSourceCode`：支持从销售订单、采购订单等来源追溯

### 1.2 工单状态流转

```
┌─────────────────────────────────────────────────────────────────┐
│                    工单状态流转图                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐ │
│   │  草稿    │───>│  已确认  │───>│  进行中  │───>│  已完成  │ │
│   └──────────┘    └──────────┘    └──────────┘    └──────────┘ │
│        │               │               │                       │
│        │               │               │                       │
│        ▼               ▼               ▼                       │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐                 │
│   │  已取消  │    │  已取消  │    │  已取消  │                 │
│   └──────────┘    └──────────┘    └──────────┘                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**状态枚举定义**：

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/enums/pro/MesProWorkOrderStatusEnum.java

public enum MesProWorkOrderStatusEnum {

    DRAFT(0, "草稿"),
    CONFIRMED(1, "已确认"),
    IN_PROGRESS(2, "进行中"),
    FINISHED(3, "已完成"),
    CANCELLED(4, "已取消");

    private final Integer status;
    private final String name;
}
```

### 1.3 工单服务层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/service/pro/workorder/MesProWorkOrderService.java

public interface MesProWorkOrderService {

    // ========== 基础 CRUD ==========
    
    Long createWorkOrder(@Valid MesProWorkOrderSaveReqVO createReqVO);
    void updateWorkOrder(@Valid MesProWorkOrderSaveReqVO updateReqVO);
    void deleteWorkOrder(Long id);
    MesProWorkOrderDO getWorkOrder(Long id);
    PageResult<MesProWorkOrderDO> getWorkOrderPage(MesProWorkOrderPageReqVO pageReqVO);
    
    // ========== 工单状态流转 ==========
    
    /**
     * 确认工单
     * 状态从 草稿 → 已确认
     */
    void confirmWorkOrder(Long id);
    
    /**
     * 完成工单
     * 状态从 进行中 → 已完成
     */
    void finishWorkOrder(Long id);
    
    /**
     * 取消工单
     * 状态从 草稿/已确认/进行中 → 已取消
     */
    void cancelWorkOrder(Long id);
    
    // ========== 工单数量管理 ==========
    
    /**
     * 更新已生产数量（报工时调用）
     */
    void updateQuantityProduced(Long workOrderId, BigDecimal quantity);
    
    /**
     * 更新已排产数量（排产时调用）
     */
    void updateQuantityScheduled(Long workOrderId, BigDecimal quantity);
    
    // ========== 工单查询 ==========
    
    List<MesProWorkOrderDO> getWorkOrderListByStatus(Integer status);
    List<MesProWorkOrderDO> getWorkOrderListByProductId(Long productId);
    Map<Long, MesProWorkOrderDO> getWorkOrderMap(Collection<Long> ids);
}
```

### 1.4 工单控制器层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/controller/admin/pro/workorder/MesProWorkOrderController.java

@Tag(name = "管理后台 - MES 生产工单")
@RestController
@RequestMapping("/mes/pro/work-order")
@Validated
public class MesProWorkOrderController {

    @Resource
    private MesProWorkOrderService workOrderService;
    @Resource
    private MesMdItemService itemService;
    @Resource
    private MesMdClientService clientService;
    @Resource
    private MesMdVendorService vendorService;
    @Resource
    private MesMdUnitMeasureService unitMeasureService;

    @PostMapping("/create")
    @Operation(summary = "创建生产工单")
    @PreAuthorize("@ss.hasPermission('mes:pro-work-order:create')")
    public CommonResult<Long> createWorkOrder(@Valid @RequestBody MesProWorkOrderSaveReqVO createReqVO) {
        return success(workOrderService.createWorkOrder(createReqVO));
    }

    @PutMapping("/confirm")
    @Operation(summary = "确认工单")
    @PreAuthorize("@ss.hasPermission('mes:pro-work-order:update')")
    public CommonResult<Boolean> confirmWorkOrder(@RequestParam("id") Long id) {
        workOrderService.confirmWorkOrder(id);
        return success(true);
    }

    @PutMapping("/finish")
    @Operation(summary = "完成工单")
    @PreAuthorize("@ss.hasPermission('mes:pro-work-order:update')")
    public CommonResult<Boolean> finishWorkOrder(@RequestParam("id") Long id) {
        workOrderService.finishWorkOrder(id);
        return success(true);
    }

    @PutMapping("/cancel")
    @Operation(summary = "取消工单")
    @PreAuthorize("@ss.hasPermission('mes:pro-work-order:update')")
    public CommonResult<Boolean> cancelWorkOrder(@RequestParam("id") Long id) {
        workOrderService.cancelWorkOrder(id);
        return success(true);
    }

    @GetMapping("/get")
    @Operation(summary = "获得生产工单")
    @PreAuthorize("@ss.hasPermission('mes:pro-work-order:query')")
    public CommonResult<MesProWorkOrderRespVO> getWorkOrder(@RequestParam("id") Long id) {
        MesProWorkOrderDO workOrder = workOrderService.getWorkOrder(id);
        if (workOrder == null) {
            return success(null);
        }
        return success(buildWorkOrderRespVOList(ListUtil.of(workOrder)).get(0));
    }

    // ==================== 拼接 VO ====================

    private List<MesProWorkOrderRespVO> buildWorkOrderRespVOList(List<MesProWorkOrderDO> list) {
        if (CollUtil.isEmpty(list)) {
            return Collections.emptyList();
        }
        // 1. 批量获取关联数据
        Map<Long, MesMdItemDO> itemMap = itemService.getItemMap(
                convertSet(list, MesProWorkOrderDO::getProductId));
        Map<Long, MesMdClientDO> clientMap = clientService.getClientMap(
                convertSet(list, MesProWorkOrderDO::getClientId));
        Map<Long, MesMdVendorDO> vendorMap = vendorService.getVendorMap(
                convertSet(list, MesProWorkOrderDO::getVendorId));
        // 单位从产品关联的 item 获得
        Map<Long, MesMdUnitMeasureDO> unitMeasureMap = unitMeasureService.getUnitMeasureMap(
                convertSet(itemMap.values(), MesMdItemDO::getUnitMeasureId));
        Map<Long, MesProWorkOrderDO> parentMap = workOrderService.getWorkOrderMap(
                convertSet(list, MesProWorkOrderDO::getParentId));
        
        // 2. 拼接 VO
        return BeanUtils.toBean(list, MesProWorkOrderRespVO.class, vo -> {
            MapUtils.findAndThen(itemMap, vo.getProductId(), item -> {
                vo.setProductName(item.getName()).setProductCode(item.getCode())
                        .setProductSpecification(item.getSpecification());
                MapUtils.findAndThen(unitMeasureMap, item.getUnitMeasureId(),
                        unitMeasure -> vo.setUnitMeasureName(unitMeasure.getName()));
            });
            MapUtils.findAndThen(clientMap, vo.getClientId(),
                    client -> vo.setClientName(client.getName()).setClientCode(client.getCode()));
            MapUtils.findAndThen(vendorMap, vo.getVendorId(),
                    vendor -> vo.setVendorName(vendor.getName()).setVendorCode(vendor.getCode()));
            MapUtils.findAndThen(parentMap, vo.getParentId(),
                    parent -> vo.setParentCode(parent.getCode()));
        });
    }
}
```

**API 设计要点**：
- 状态流转独立 API：`/confirm`、`/finish`、`/cancel`
- 批量获取关联数据：产品、客户、供应商、父工单
- 使用 `MapUtils.findAndThen` 高效填充 VO

---

## 2. 工单 BOM 管理

### 2.1 BOM 数据模型

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/dal/dataobject/pro/workorder/MesProWorkOrderBomDO.java

@TableName("mes_pro_work_order_bom")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesProWorkOrderBomDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * 工单编号，关联 MesProWorkOrderDO
     */
    private Long workOrderId;
    
    /**
     * 物料编号，关联 MesMdItemDO
     */
    private Long itemId;
    
    /**
     * 需求数量
     */
    private BigDecimal quantity;
    
    /**
     * 已领数量
     */
    private BigDecimal quantityIssued;
    
    /**
     * 单位编号，关联 MesMdUnitMeasureDO
     */
    private Long unitMeasureId;
    
    /**
     * 备注
     */
    private String remark;
}
```

### 2.2 BOM 服务层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/service/pro/workorder/MesProWorkOrderBomService.java

public interface MesProWorkOrderBomService {

    Long createWorkOrderBom(@Valid MesProWorkOrderBomSaveReqVO createReqVO);
    void updateWorkOrderBom(@Valid MesProWorkOrderBomSaveReqVO updateReqVO);
    void deleteWorkOrderBom(Long id);
    MesProWorkOrderBomDO getWorkOrderBom(Long id);
    PageResult<MesProWorkOrderBomDO> getWorkOrderBomPage(MesProWorkOrderBomPageReqVO pageReqVO);
    
    /**
     * 根据工单编号获得 BOM 列表
     */
    List<MesProWorkOrderBomDO> getWorkOrderBomListByWorkOrderId(Long workOrderId);
    
    /**
     * 根据工单编号删除 BOM
     */
    void deleteWorkOrderBomByWorkOrderId(Long workOrderId);
    
    /**
     * 根据产品 BOM 自动生成工单 BOM 行
     * 
     * @param workOrderId 工单编号
     * @param reqVO 工单创建/更新请求（取 productId、quantity）
     * @param updated 是否为更新场景（true 时先删除旧 BOM）
     */
    void generateWorkOrderBom(Long workOrderId, MesProWorkOrderSaveReqVO reqVO, boolean updated);
}
```

**BOM 自动生成逻辑**：
1. 从产品 BOM（`MesMdProductBomDO`）获取物料清单
2. 根据工单生产数量计算每种物料的需求数量
3. 批量创建工单 BOM 行

### 2.3 BOM 控制器层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/controller/admin/pro/workorder/MesProWorkOrderBomController.java

@Tag(name = "管理后台 - MES 工单 BOM")
@RestController
@RequestMapping("/mes/pro/work-order-bom")
@Validated
public class MesProWorkOrderBomController {

    @Resource
    private MesProWorkOrderBomService workOrderBomService;
    @Resource
    private MesMdItemService itemService;
    @Resource
    private MesMdUnitMeasureService unitMeasureService;

    @PostMapping("/create")
    @Operation(summary = "创建工单 BOM")
    @PreAuthorize("@ss.hasPermission('mes:pro-work-order-bom:create')")
    public CommonResult<Long> createWorkOrderBom(@Valid @RequestBody MesProWorkOrderBomSaveReqVO createReqVO) {
        return success(workOrderBomService.createWorkOrderBom(createReqVO));
    }

    @GetMapping("/list")
    @Operation(summary = "获得工单 BOM 列表")
    @PreAuthorize("@ss.hasPermission('mes:pro-work-order-bom:query')")
    public CommonResult<List<MesProWorkOrderBomRespVO>> getWorkOrderBomList(
            @RequestParam("workOrderId") Long workOrderId) {
        List<MesProWorkOrderBomDO> list = workOrderBomService.getWorkOrderBomListByWorkOrderId(workOrderId);
        return success(buildWorkOrderBomRespVOList(list));
    }

    // ==================== 拼接 VO ====================

    private List<MesProWorkOrderBomRespVO> buildWorkOrderBomRespVOList(List<MesProWorkOrderBomDO> list) {
        if (CollUtil.isEmpty(list)) {
            return Collections.emptyList();
        }
        // 批量获取物料信息
        Map<Long, MesMdItemDO> itemMap = itemService.getItemMap(convertSet(list, MesProWorkOrderBomDO::getItemId));
        Map<Long, MesMdUnitMeasureDO> unitMeasureMap = unitMeasureService.getUnitMeasureMap(
                convertSet(list, MesProWorkOrderBomDO::getUnitMeasureId));
        
        return BeanUtils.toBean(list, MesProWorkOrderBomRespVO.class, vo -> {
            MapUtils.findAndThen(itemMap, vo.getItemId(), item -> 
                vo.setItemName(item.getName()).setItemCode(item.getCode()));
            MapUtils.findAndThen(unitMeasureMap, vo.getUnitMeasureId(), unit -> 
                vo.setUnitMeasureName(unit.getName()));
        });
    }
}
```

---

## 3. 生产报工实现

### 3.1 报工数据模型

报工记录通常关联到工单的工序执行：

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/dal/dataobject/pro/report/MesProReportDO.java

@TableName("mes_pro_report")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesProReportDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * 报工编码
     */
    private String code;
    
    /**
     * 工单编号，关联 MesProWorkOrderDO
     */
    private Long workOrderId;
    
    /**
     * 工序编号，关联 MesProProcessDO
     */
    private Long processId;
    
    /**
     * 报工数量
     */
    private BigDecimal quantity;
    
    /**
     * 合格数量
     */
    private BigDecimal quantityQualified;
    
    /**
     * 不合格数量
     */
    private BigDecimal quantityUnqualified;
    
    /**
     * 报工人编号，关联 AdminUserDO
     */
    private Long reportUserId;
    
    /**
     * 报工时间
     */
    private LocalDateTime reportTime;
    
    /**
     * 报工状态
     */
    private Integer status;
    
    /**
     * 备注
     */
    private String remark;
}
```

### 3.2 报工服务层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/service/pro/report/MesProReportService.java

public interface MesProReportService {

    /**
     * 创建报工记录
     * 
     * 1. 校验工单状态（必须是进行中）
     * 2. 创建报工记录
     * 3. 更新工单已生产数量
     * 4. 检查工单是否完成
     */
    Long createReport(@Valid MesProReportSaveReqVO createReqVO);
    
    void updateReport(@Valid MesProReportSaveReqVO updateReqVO);
    void deleteReport(Long id);
    MesProReportDO getReport(Long id);
    PageResult<MesProReportDO> getReportPage(MesProReportPageReqVO pageReqVO);
    
    /**
     * 获取工单的报工记录列表
     */
    List<MesProReportDO> getReportListByWorkOrderId(Long workOrderId);
    
    /**
     * 获取工单的报工统计
     * 
     * @return 报工统计（总数量、合格数量、不合格数量）
     */
    MesProReportSummaryVO getReportSummaryByWorkOrderId(Long workOrderId);
}
```

### 3.3 报工业务流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      报工业务流程                                │
├─────────────────────────────────────────────────────────────────┤
│ 1. 工人选择工单和工序                                            │
│ 2. 填写报工数量、合格数量                                        │
│ 3. 提交报工                                                      │
│ 4. 系统校验：                                                    │
│    - 工单状态是否为"进行中"                                      │
│    - 报工数量是否超过剩余数量                                    │
│ 5. 创建报工记录                                                  │
│ 6. 更新工单已生产数量（quantityProduced += quantity）             │
│ 7. 检查工单是否完成（quantityProduced >= quantity）               │
│ 8. 如果完成，自动触发工单完成流程                                │
└─────────────────────────────────────────────────────────────────┘
```

### 3.4 报工控制器层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/controller/admin/pro/report/MesProReportController.java

@Tag(name = "管理后台 - MES 生产报工")
@RestController
@RequestMapping("/mes/pro/report")
@Validated
public class MesProReportController {

    @Resource
    private MesProReportService reportService;
    @Resource
    private MesProWorkOrderService workOrderService;

    @PostMapping("/create")
    @Operation(summary = "创建报工记录")
    @PreAuthorize("@ss.hasPermission('mes:pro-report:create')")
    public CommonResult<Long> createReport(@Valid @RequestBody MesProReportSaveReqVO createReqVO) {
        return success(reportService.createReport(createReqVO));
    }

    @GetMapping("/page")
    @Operation(summary = "获得报工记录分页")
    @PreAuthorize("@ss.hasPermission('mes:pro-report:query')")
    public CommonResult<PageResult<MesProReportRespVO>> getReportPage(@Valid MesProReportPageReqVO pageReqVO) {
        PageResult<MesProReportDO> pageResult = reportService.getReportPage(pageReqVO);
        return success(new PageResult<>(buildReportRespVOList(pageResult.getList()), pageResult.getTotal()));
    }

    @GetMapping("/summary")
    @Operation(summary = "获得工单的报工统计")
    @PreAuthorize("@ss.hasPermission('mes:pro-report:query')")
    public CommonResult<MesProReportSummaryVO> getReportSummary(
            @RequestParam("workOrderId") Long workOrderId) {
        return success(reportService.getReportSummaryByWorkOrderId(workOrderId));
    }
}
```

---

## 4. 质量检验实现

### 4.1 质检单数据模型

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/dal/dataobject/qc/MesQcOrderDO.java

@TableName("mes_qc_order")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesQcOrderDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * 质检单编码
     */
    private String code;
    
    /**
     * 质检类型（来料质检、过程质检、成品质检）
     */
    private Integer type;
    
    /**
     * 来源类型（工单、采购单等）
     */
    private Integer sourceType;
    
    /**
     * 来源单据编号
     */
    private String sourceCode;
    
    /**
     * 工单编号，关联 MesProWorkOrderDO
     */
    private Long workOrderId;
    
    /**
     * 物料编号，关联 MesMdItemDO
     */
    private Long itemId;
    
    /**
     * 质检数量
     */
    private BigDecimal quantity;
    
    /**
     * 合格数量
     */
    private BigDecimal quantityQualified;
    
    /**
     * 不合格数量
     */
    private BigDecimal quantityUnqualified;
    
    /**
     * 质检员编号，关联 AdminUserDO
     */
    private Long inspectorId;
    
    /**
     * 质检时间
     */
    private LocalDateTime inspectTime;
    
    /**
     * 质检结果（合格、不合格、让步接收）
     */
    private Integer result;
    
    /**
     * 质检状态
     */
    private Integer status;
}
```

### 4.2 质检服务层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/service/qc/MesQcOrderService.java

public interface MesQcOrderService {

    Long createQcOrder(@Valid MesQcOrderSaveReqVO createReqVO);
    void updateQcOrder(@Valid MesQcOrderSaveReqVO updateReqVO);
    void deleteQcOrder(Long id);
    MesQcOrderDO getQcOrder(Long id);
    PageResult<MesQcOrderDO> getQcOrderPage(MesQcOrderPageReqVO pageReqVO);
    
    /**
     * 执行质检
     * 
     * 1. 记录质检结果（合格/不合格数量）
     * 2. 更新质检状态
     * 3. 如果是成品质检，更新工单状态
     */
    void inspectQcOrder(@Valid MesQcOrderInspectReqVO reqVO);
    
    /**
     * 获取工单的质检记录
     */
    List<MesQcOrderDO> getQcOrderListByWorkOrderId(Long workOrderId);
}
```

### 4.3 质检业务流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      质检业务流程                                │
├─────────────────────────────────────────────────────────────────┤
│ 1. 创建质检单（自动或手动）                                      │
│    - 来料质检：采购入库时自动创建                                │
│    - 过程质检：工序完成时自动创建                                │
│    - 成品质检：工单完成时自动创建                                │
│ 2. 质检员执行质检                                                │
│ 3. 填写质检结果：                                                │
│    - 合格数量                                                    │
│    - 不合格数量                                                  │
│    - 不合格原因                                                  │
│ 4. 判定质检结果：                                                │
│    - 全部合格 → 合格                                            │
│    - 部分合格 → 让步接收（可选）                                 │
│    - 全部不合格 → 不合格                                        │
│ 5. 后续处理：                                                    │
│    - 合格：正常流转                                              │
│    - 不合格：退货/返工/报废                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 产品 BOM 管理

### 5.1 产品 BOM 数据模型

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/dal/dataobject/md/item/MesMdProductBomDO.java

@TableName("mes_md_product_bom")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesMdProductBomDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * BOM 编码
     */
    private String code;
    
    /**
     * BOM 名称
     */
    private String name;
    
    /**
     * 产品编号，关联 MesMdItemDO
     */
    private Long productId;
    
    /**
     * 版本号
     */
    private String version;
    
    /**
     * 是否默认版本
     */
    private Boolean defaultVersion;
    
    /**
     * 状态
     */
    private Integer status;
}
```

### 5.2 BOM 明细数据模型

```java
// BOM 明细行
@TableName("mes_md_product_bom_line")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesMdProductBomLineDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * BOM 编号，关联 MesMdProductBomDO
     */
    private Long bomId;
    
    /**
     * 物料编号，关联 MesMdItemDO
     */
    private Long itemId;
    
    /**
     * 用量
     */
    private BigDecimal quantity;
    
    /**
     * 损耗率
     */
    private BigDecimal scrapRate;
    
    /**
     * 单位编号，关联 MesMdUnitMeasureDO
     */
    private Long unitMeasureId;
}
```

### 5.3 BOM 控制器层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/controller/admin/md/item/MesMdProductBomController.java

@Tag(name = "管理后台 - MES 产品 BOM")
@RestController
@RequestMapping("/mes/md/product-bom")
@Validated
public class MesMdProductBomController {

    @Resource
    private MesMdProductBomService productBomService;

    @PostMapping("/create")
    @Operation(summary = "创建产品 BOM")
    @PreAuthorize("@ss.hasPermission('mes:md-product-bom:create')")
    public CommonResult<Long> createProductBom(@Valid @RequestBody MesMdProductBomSaveReqVO createReqVO) {
        return success(productBomService.createProductBom(createReqVO));
    }

    @GetMapping("/list")
    @Operation(summary = "获得产品 BOM 列表")
    @PreAuthorize("@ss.hasPermission('mes:md-product-bom:query')")
    public CommonResult<List<MesMdProductBomRespVO>> getProductBomList(
            @RequestParam("productId") Long productId) {
        List<MesMdProductBomDO> list = productBomService.getProductBomListByProductId(productId);
        return success(BeanUtils.toBean(list, MesMdProductBomRespVO.class));
    }

    @GetMapping("/default")
    @Operation(summary = "获得产品的默认 BOM")
    @PreAuthorize("@ss.hasPermission('mes:md-product-bom:query')")
    public CommonResult<MesMdProductBomRespVO> getDefaultProductBom(
            @RequestParam("productId") Long productId) {
        MesMdProductBomDO bom = productBomService.getDefaultProductBomByProductId(productId);
        return success(BeanUtils.toBean(bom, MesMdProductBomRespVO.class));
    }
}
```

---

## 6. 工序与工艺路线

### 6.1 工序数据模型

```java
// 工序定义
@TableName("mes_pro_process")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesProProcessDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * 工序编码
     */
    private String code;
    
    /**
     * 工序名称
     */
    private String name;
    
    /**
     * 工序类型
     */
    private Integer type;
    
    /**
     * 标准工时（分钟）
     */
    private BigDecimal standardTime;
    
    /**
     * 设备编号，关联 MesDvMachineryDO
     */
    private Long machineryId;
    
    /**
     * 工序描述
     */
    private String description;
}
```

### 6.2 工艺路线数据模型

```java
// 工艺路线
@TableName("mes_pro_route")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesProRouteDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * 工艺路线编码
     */
    private String code;
    
    /**
     * 工艺路线名称
     */
    private String name;
    
    /**
     * 产品编号，关联 MesMdItemDO
     */
    private Long productId;
    
    /**
     * 版本号
     */
    private String version;
    
    /**
     * 是否默认版本
     */
    private Boolean defaultVersion;
    
    /**
     * 状态
     */
    private Integer status;
}

// 工艺路线工序
@TableName("mes_pro_route_process")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesProRouteProcessDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * 工艺路线编号，关联 MesProRouteDO
     */
    private Long routeId;
    
    /**
     * 工序编号，关联 MesProProcessDO
     */
    private Long processId;
    
    /**
     * 工序顺序
     */
    private Integer sort;
    
    /**
     * 标准工时（分钟）
     */
    private BigDecimal standardTime;
}
```

---

## 7. 排产管理

### 7.1 排产计划数据模型

```java
// 排产计划
@TableName("mes_pro_schedule")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class MesProScheduleDO extends BaseDO {

    @TableId
    private Long id;
    
    /**
     * 排产编码
     */
    private String code;
    
    /**
     * 工单编号，关联 MesProWorkOrderDO
     */
    private Long workOrderId;
    
    /**
     * 工序编号，关联 MesProProcessDO
     */
    private Long processId;
    
    /**
     * 计划数量
     */
    private BigDecimal quantity;
    
    /**
     * 计划开始时间
     */
    private LocalDateTime planStartTime;
    
    /**
     * 计划结束时间
     */
    private LocalDateTime planEndTime;
    
    /**
     * 实际开始时间
     */
    private LocalDateTime actualStartTime;
    
    /**
     * 实际结束时间
     */
    private LocalDateTime actualEndTime;
    
    /**
     * 设备编号，关联 MesDvMachineryDO
     */
    private Long machineryId;
    
    /**
     * 班组编号，关联 MesCalTeamDO
     */
    private Long teamId;
    
    /**
     * 排产状态
     */
    private Integer status;
}
```

### 7.2 排产服务层实现

```java
// 文件位置：yudao-module-mes/yudao-module-mes-server/src/main/java/cn/iocoder/yudao/module/mes/service/pro/schedule/MesProScheduleService.java

public interface MesProScheduleService {

    /**
     * 创建排产计划
     * 
     * 1. 校验工单状态
     * 2. 校验排产数量不超过剩余数量
     * 3. 创建排产记录
     * 4. 更新工单已排产数量
     */
    Long createSchedule(@Valid MesProScheduleSaveReqVO createReqVO);
    
    void updateSchedule(@Valid MesProScheduleSaveReqVO updateReqVO);
    void deleteSchedule(Long id);
    MesProScheduleDO getSchedule(Long id);
    PageResult<MesProScheduleDO> getSchedulePage(MesProSchedulePageReqVO pageReqVO);
    
    /**
     * 获取工单的排产计划列表
     */
    List<MesProScheduleDO> getScheduleListByWorkOrderId(Long workOrderId);
    
    /**
     * 获取设备的排产计划列表（用于设备甘特图）
     */
    List<MesProScheduleDO> getScheduleListByMachineryId(Long machineryId, LocalDateTime startTime, LocalDateTime endTime);
}
```

---

## 总结

### 核心实现要点

1. **工单管理**：完整的状态流转 + 数量跟踪 + 父子工单支持
2. **BOM 管理**：产品 BOM → 工单 BOM 自动转换
3. **生产报工**：实时更新工单进度 + 自动完成检测
4. **质量检验**：支持来料/过程/成品三种质检类型
5. **排产管理**：支持按设备/班组排产 + 甘特图展示

### 关键业务流程

| 流程 | 核心步骤 | 关键服务 |
|------|---------|---------|
| 工单创建 | 创建工单 → 自动生成 BOM | `MesProWorkOrderService` + `MesProWorkOrderBomService` |
| 生产报工 | 选择工单/工序 → 填写数量 → 更新工单进度 | `MesProReportService` |
| 质量检验 | 创建质检单 → 执行质检 → 判定结果 | `MesQcOrderService` |
| 排产计划 | 选择工单/工序 → 分配设备/班组 → 设定时间 | `MesProScheduleService` |

### 数据关联关系

```
┌─────────────────────────────────────────────────────────────────┐
│                      MES 核心数据关系                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   产品(BOM) ──────> 工单 ──────> 排产计划                       │
│      │                │              │                          │
│      │                │              ▼                          │
│      │                │          工序执行                        │
│      │                │              │                          │
│      │                ▼              ▼                          │
│      │            报工记录 <──── 质检单                          │
│      │                                                          │
│      └────────────────────────────────────────────────────────  │
│                        BOM 明细（物料清单）                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

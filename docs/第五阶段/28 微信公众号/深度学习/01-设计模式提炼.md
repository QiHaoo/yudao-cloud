# 微信公众号模块设计模式提炼

## 一、SDK 封装的适配器模式

### 1.1 什么是 SDK 封装

微信公众号开发需要使用微信官方 SDK（WxJava），但直接使用 SDK 会带来以下问题：

- **多账号管理**：一个系统可能管理多个公众号
- **配置复杂**：每个账号有不同的 AppId、Secret
- **API 差异**：不同版本的 SDK 接口可能不同

适配器模式的核心是**统一接口 + 实现隔离**。

```
┌─────────────────────────────────────────────────────────────────┐
│                      SDK 适配器架构                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                      业务层                              │  │
│   │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │  │
│   │  │ 消息管理 │  │ 菜单管理 │  │ 用户管理 │              │  │
│   │  └────┬─────┘  └────┬─────┘  └────┬─────┘              │  │
│   │       │              │              │                    │  │
│   └───────┼──────────────┼──────────────┼────────────────────┘  │
│           │              │              │                        │
│           ▼              ▼              ▼                        │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                   适配器层                               │  │
│   │              MpServiceFactory                           │  │
│   └─────────────────────────┬───────────────────────────────┘  │
│                             │                                   │
│                             ▼                                   │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    微信 SDK 层                           │  │
│   │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │  │
│   │  │ WxMpService│ │ WxMpMenuService│ │ WxMpUserService│  │  │
│   │  └──────────┘  └──────────┘  └──────────┘              │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 设计模式：工厂模式 + 策略模式

项目采用**工厂模式**创建和管理多个公众号的 WxMpService 实例。

**源码位置**：`yudao-module-mp-server/.../framework/mp/core/DefaultMpServiceFactory.java`

```java
/**
 * 默认的 {@link MpServiceFactory} 实现类
 */
@Slf4j
@RequiredArgsConstructor
public class DefaultMpServiceFactory implements MpServiceFactory {

    /**
     * 微信 appId 与 WxMpService 的映射
     */
    private volatile Map<String, WxMpService> appId2MpServices;
    /**
     * 公众号账号 id 与 WxMpService 的映射
     */
    private volatile Map<Long, WxMpService> id2MpServices;
    /**
     * 微信 appId 与 WxMpMessageRouter 的映射
     */
    private volatile Map<String, WxMpMessageRouter> mpMessageRouters;

    private final RedisTemplateWxRedisOps redisTemplateWxRedisOps;
    private final WxMpProperties mpProperties;

    @Override
    public void init(List<MpAccountDO> list) {
        Map<String, WxMpService> appId2MpServices = Maps.newHashMap();
        Map<Long, WxMpService> id2MpServices = Maps.newHashMap();
        Map<String, WxMpMessageRouter> mpMessageRouters = Maps.newHashMap();

        // 处理 list
        list.forEach(account -> {
            // 构建 WxMpService 对象
            WxMpService mpService = buildMpService(account);
            appId2MpServices.put(account.getAppId(), mpService);
            id2MpServices.put(account.getId(), mpService);
            // 构建 WxMpMessageRouter 对象
            WxMpMessageRouter mpMessageRouter = buildMpMessageRouter(mpService);
            mpMessageRouters.put(account.getAppId(), mpMessageRouter);
        });

        // 设置到缓存
        this.appId2MpServices = appId2MpServices;
        this.id2MpServices = id2MpServices;
        this.mpMessageRouters = mpMessageRouters;
    }

    @Override
    public WxMpService getMpService(Long id) {
        return id2MpServices.get(id);
    }

    @Override
    public WxMpService getMpService(String appId) {
        return appId2MpServices.get(appId);
    }

    @Override
    public WxMpMessageRouter getMpMessageRouter(String appId) {
        return mpMessageRouters.get(appId);
    }

    private WxMpService buildMpService(MpAccountDO account) {
        // 第一步，创建 WxMpRedisConfigImpl 对象
        WxMpRedisConfigImpl configStorage = new WxMpRedisConfigImpl(
                redisTemplateWxRedisOps, mpProperties.getConfigStorage().getKeyPrefix());
        configStorage.setAppId(account.getAppId());
        configStorage.setSecret(account.getAppSecret());
        configStorage.setToken(account.getToken());
        configStorage.setAesKey(account.getAesKey());

        // 第二步，创建 WxMpService 对象
        WxMpService service = new WxMpServiceImpl();
        service.setWxMpConfigStorage(configStorage);
        return service;
    }
}
```

### 1.3 配置存储设计

项目使用**Redis 存储配置**，支持分布式环境下的配置共享：

```java
private WxMpService buildMpService(MpAccountDO account) {
    // 使用 Redis 存储配置，支持分布式
    WxMpRedisConfigImpl configStorage = new WxMpRedisConfigImpl(
            redisTemplateWxRedisOps, mpProperties.getConfigStorage().getKeyPrefix());
    configStorage.setAppId(account.getAppId());
    configStorage.setSecret(account.getAppSecret());
    configStorage.setToken(account.getToken());
    configStorage.setAesKey(account.getAesKey());

    WxMpService service = new WxMpServiceImpl();
    service.setWxMpConfigStorage(configStorage);
    return service;
}
```

### 1.4 可迁移经验

**设计要点**：

| 设计决策 | 理由 |
|---------|------|
| 工厂模式 | 统一管理多个 SDK 实例 |
| Map 缓存 | 快速查找对应的 SDK 实例 |
| Redis 配置存储 | 支持分布式环境 |
| 延迟初始化 | 按需创建 SDK 实例 |

**迁移建议**：任何需要"多账号管理"的场景都可以使用此模式。例如：多租户系统、多店铺管理、多渠道对接。

---

## 二、消息路由的策略模式

### 2.1 什么是消息路由

微信公众号会收到各种类型的消息：文本消息、图片消息、事件推送等。消息路由的作用是将不同类型的消息分发给对应的处理器。

```
┌─────────────────────────────────────────────────────────────────┐
│                      消息路由架构                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    微信服务器                            │  │
│   └─────────────────────────┬───────────────────────────────┘  │
│                             │                                   │
│                             ▼                                   │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                   消息路由器                             │  │
│   │               WxMpMessageRouter                         │  │
│   └─────────────────────────┬───────────────────────────────┘  │
│                             │                                   │
│          ┌──────────────────┼──────────────────┐               │
│          │                  │                  │               │
│          ▼                  ▼                  ▼               │
│   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐        │
│   │ 文本消息    │   │ 事件消息    │   │ 图片消息    │        │
│   │ 处理器      │   │ 处理器      │   │ 处理器      │        │
│   └─────────────┘   └─────────────┘   └─────────────┘        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 设计模式：规则链模式

项目采用**规则链**模式，通过链式规则配置实现消息路由。

**源码位置**：`yudao-module-mp-server/.../framework/mp/core/DefaultMpServiceFactory.java`

```java
private WxMpMessageRouter buildMpMessageRouter(WxMpService mpService) {
    WxMpMessageRouter router = new WxMpMessageRouter(mpService);

    // 记录所有事件的日志（异步执行）
    router.rule().handler(messageReceiveHandler).next();

    // 接收客服会话管理事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxMpEventConstants.CustomerService.KF_CREATE_SESSION)
            .handler(kfSessionHandler).end();
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxMpEventConstants.CustomerService.KF_CLOSE_SESSION)
            .handler(kfSessionHandler).end();
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxMpEventConstants.CustomerService.KF_SWITCH_SESSION)
            .handler(kfSessionHandler).end();

    // 门店审核事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxMpEventConstants.POI_CHECK_NOTIFY)
            .handler(storeCheckNotifyHandler).end();

    // 自定义菜单事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxConsts.MenuButtonType.CLICK)
            .handler(menuHandler).end();

    // 点击菜单连接事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxConsts.MenuButtonType.VIEW)
            .handler(nullHandler).end();

    // 关注事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxConsts.EventType.SUBSCRIBE)
            .handler(subscribeHandler).end();

    // 取消关注事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxConsts.EventType.UNSUBSCRIBE)
            .handler(unsubscribeHandler).end();

    // 上报地理位置事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxConsts.EventType.LOCATION)
            .handler(locationHandler).end();

    // 接收地理位置消息
    router.rule().async(false).msgType(WxConsts.XmlMsgType.LOCATION)
            .handler(locationHandler).end();

    // 扫码事件
    router.rule().async(false).msgType(WxConsts.XmlMsgType.EVENT)
            .event(WxConsts.EventType.SCAN)
            .handler(scanHandler).end();

    // 默认
    router.rule().async(false).handler(messageAutoReplyHandler).end();
    return router;
}
```

### 2.3 处理器接口

每个处理器实现统一的接口：

```java
// 消息接收处理器
public class MessageReceiveHandler implements WxMpMessageHandler {
    @Override
    public WxMpXmlOutMessage handle(WxMpXmlMessage wxMessage,
                                     Map<String, Object> context,
                                     WxMpService wxMpService,
                                     WxMpSessionManager sessionManager) {
        // 记录消息日志
        return null;
    }
}

// 关注处理器
public class SubscribeHandler implements WxMpMessageHandler {
    @Override
    public WxMpXmlOutMessage handle(WxMpXmlMessage wxMessage,
                                     Map<String, Object> context,
                                     WxMpService wxMpService,
                                     WxMpSessionManager sessionManager) {
        // 处理关注事件
        return WxMpXmlOutMessage.TEXT()
                .content("欢迎关注！")
                .fromUser(wxMessage.getToUser())
                .toUser(wxMessage.getFromUser())
                .build();
    }
}
```

### 2.4 可迁移经验

**设计要点**：

| 设计决策 | 理由 |
|---------|------|
| 规则链模式 | 灵活配置消息路由规则 |
| 异步处理 | 非关键消息异步处理，提升性能 |
| 处理器分离 | 每种消息类型一个处理器，职责单一 |
| 默认处理器 | 未匹配的消息走默认处理逻辑 |

**迁移建议**：任何需要"消息分发"的场景都可以使用此模式。例如：事件总线、消息队列消费者、Webhook 处理。

---

## 三、Access Token 的缓存与刷新策略

### 3.1 什么是 Access Token

Access Token 是调用微信 API 的凭证，有效期 2 小时。需要缓存并定期刷新，避免频繁请求微信服务器。

```
┌─────────────────────────────────────────────────────────────────┐
│                    Access Token 生命周期                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────┐                                              │
│   │  首次请求   │                                              │
│   └──────┬──────┘                                              │
│          │                                                     │
│          ▼                                                     │
│   ┌─────────────┐     ┌─────────────┐                          │
│   │  获取 Token │ ──> │  缓存 Token │                          │
│   └──────┬──────┘     └──────┬──────┘                          │
│          │                   │                                  │
│          │                   ▼                                  │
│          │            ┌─────────────┐                          │
│          │            │  2小时有效  │                          │
│          │            └──────┬──────┘                          │
│          │                   │                                  │
│          │                   ▼                                  │
│          │            ┌─────────────┐                          │
│          │            │  Token 过期 │                          │
│          │            └──────┬──────┘                          │
│          │                   │                                  │
│          │                   ▼                                  │
│          │            ┌─────────────┐                          │
│          └────────────│  重新获取   │                          │
│                       └─────────────┘                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 设计模式：Redis 缓存 + 自动刷新

项目采用**Redis 缓存**存储 Access Token，利用 Redis 的过期机制自动管理 Token 生命周期。

**源码位置**：`yudao-module-mp-server/.../framework/mp/core/DefaultMpServiceFactory.java`

```java
private WxMpService buildMpService(MpAccountDO account) {
    // 使用 Redis 存储配置（包括 Access Token）
    WxMpRedisConfigImpl configStorage = new WxMpRedisConfigImpl(
            redisTemplateWxRedisOps, mpProperties.getConfigStorage().getKeyPrefix());
    configStorage.setAppId(account.getAppId());
    configStorage.setSecret(account.getAppSecret());
    configStorage.setToken(account.getToken());
    configStorage.setAesKey(account.getAesKey());

    WxMpService service = new WxMpServiceImpl();
    service.setWxMpConfigStorage(configStorage);
    return service;
}
```

### 3.3 WxMpRedisConfigImpl 的工作原理

WxJava SDK 的 `WxMpRedisConfigImpl` 实现了以下机制：

1. **Token 缓存**：首次获取 Token 后存入 Redis
2. **过期管理**：Redis key 设置过期时间（2小时）
3. **自动刷新**：Token 过期后自动重新获取
4. **分布式锁**：防止并发刷新导致重复请求

```java
// WxMpRedisConfigImpl 内部实现（简化）
public class WxMpRedisConfigImpl extends WxMpConfigImpl {
    private final RedisTemplate redisTemplate;
    private final String keyPrefix;

    @Override
    public String getAccessToken() {
        String key = keyPrefix + ":access_token:" + appId;
        String token = redisTemplate.opsForValue().get(key);
        if (token == null) {
            // 重新获取 Token
            token = fetchAccessToken();
            // 缓存到 Redis，设置过期时间
            redisTemplate.opsForValue().set(key, token, 7000, TimeUnit.SECONDS);
        }
        return token;
    }
}
```

### 3.4 可迁移经验

**设计要点**：

| 设计决策 | 理由 |
|---------|------|
| Redis 缓存 | 支持分布式环境，多实例共享 Token |
| 自动刷新 | 业务代码无需关心 Token 过期 |
| 提前过期 | 7000秒过期（实际2小时），预留刷新时间 |
| 分布式锁 | 防止并发刷新 |

**迁移建议**：任何需要"Token 管理"的场景都可以使用此模式。例如：OAuth2 Token、JWT Token、第三方 API Token。

---

## 四、第三方 SDK 集成最佳实践

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                   第三方 SDK 集成架构                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                      业务层                              │  │
│   │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │  │
│   │  │ 消息管理 │  │ 菜单管理 │  │ 用户管理 │              │  │
│   │  └────┬─────┘  └────┬─────┘  └────┬─────┘              │  │
│   └───────┼──────────────┼──────────────┼────────────────────┘  │
│           │              │              │                        │
│           ▼              ▼              ▼                        │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                   适配器层                               │  │
│   │  ┌─────────────────────────────────────────────────┐    │  │
│   │  │              MpServiceFactory                   │    │  │
│   │  │  - 多账号管理                                    │    │  │
│   │  │  - Token 管理                                    │    │  │
│   │  │  - 消息路由                                      │    │  │
│   │  └─────────────────────────────────────────────────┘    │  │
│   └─────────────────────────┬───────────────────────────────┘  │
│                             │                                   │
│                             ▼                                   │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    SDK 封装层                            │  │
│   │  ┌─────────────────────────────────────────────────┐    │  │
│   │  │              WxJava SDK                         │    │  │
│   │  │  - WxMpService                                 │    │  │
│   │  │  - WxMpMessageRouter                           │    │  │
│   │  │  - WxMpConfigStorage                           │    │  │
│   │  └─────────────────────────────────────────────────┘    │  │
│   └─────────────────────────┬───────────────────────────────┘  │
│                             │                                   │
│                             ▼                                   │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    微信服务器                            │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 核心设计模式总结

| 模式 | 应用场景 | 核心类 |
|------|---------|--------|
| **工厂模式** | 多账号管理 | DefaultMpServiceFactory |
| **适配器模式** | SDK 封装 | MpServiceFactory |
| **规则链模式** | 消息路由 | WxMpMessageRouter |
| **缓存模式** | Token 管理 | WxMpRedisConfigImpl |
| **策略模式** | 消息处理 | WxMpMessageHandler |

### 4.3 多账号管理流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      多账号管理流程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     │
│   │  账号配置   │ ──> │  初始化     │ ──> │  缓存       │     │
│   │  (数据库)   │     │  Factory    │     │  (Map)      │     │
│   └─────────────┘     └─────────────┘     └──────┬──────┘     │
│                                                   │            │
│                                                   ▼            │
│                                            ┌─────────────┐     │
│                                            │  业务调用   │     │
│                                            │  getMpService()   │
│                                            └──────┬──────┘     │
│                                                   │            │
│                                                   ▼            │
│                                            ┌─────────────┐     │
│                                            │  WxMpService│     │
│                                            │  (实例)     │     │
│                                            └─────────────┘     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.4 可迁移的设计思想

1. **工厂模式**：任何需要"多实例管理"的场景都可以使用
2. **适配器模式**：任何需要"封装第三方 SDK"的场景都可以使用
3. **规则链模式**：任何需要"消息路由"的场景都可以使用
4. **缓存模式**：任何需要"Token 管理"的场景都可以使用

---

## 思考题

1. 如果要支持"微信支付"功能，如何扩展 MpServiceFactory？
2. 消息路由规则很多时，如何优化匹配性能？
3. Access Token 刷新失败时，如何处理重试和降级？
4. 如何设计一个"模板消息"功能，支持批量发送？

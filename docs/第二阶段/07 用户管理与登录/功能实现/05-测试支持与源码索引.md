# 05 - 测试支持与源码索引

本章包含两部分：测试支持（如何为用户管理与登录功能编写测试）和源码索引（所有相关源文件的快速查找表）。

---

## 第一部分：测试支持

### 1. 测试现状

项目中已有完善的单元测试覆盖，核心测试类：

| 测试类 | 路径 | 测试内容 |
|--------|------|---------|
| `AdminAuthServiceImplTest` | `yudao-module-system-server/src/test/java/.../service/auth/` | 登录、登出、验证码、短信登录、社交登录 |
| `OAuth2TokenServiceImplTest` | `yudao-module-system-server/src/test/java/.../service/oauth2/` | 令牌创建、刷新、查询、删除 |

### 2. 测试基类选择

项目提供了两个测试基类，根据被测组件的依赖选择：

| 基类 | 适用场景 | 提供的能力 |
|------|---------|-----------|
| `BaseDbUnitTest` | 只需要数据库的测试 | H2 内存数据库 + MyBatis-Plus |
| `BaseDbAndRedisUnitTest` | 需要数据库 + Redis 的测试 | H2 + 嵌入式 Redis |

- `AdminAuthServiceImplTest` 继承 `BaseDbUnitTest`，因为它的依赖（UserService、OAuth2TokenService 等）都用 MockBean
- `OAuth2TokenServiceImplTest` 继承 `BaseDbAndRedisUnitTest`，因为它需要真实的 Redis 来测试令牌缓存

### 3. AdminAuthServiceImplTest 测试示例

> 源码路径：`yudao-module-system/yudao-module-system-server/src/test/java/cn/iocoder/yudao/module/system/service/auth/AdminAuthServiceImplTest.java`

#### 3.1 测试类结构

```java
@Import(AdminAuthServiceImpl.class)
public class AdminAuthServiceImplTest extends BaseDbUnitTest {

    @Resource
    private AdminAuthServiceImpl authService;

    @MockBean
    private AdminUserService userService;
    @MockBean
    private CaptchaService captchaService;
    @MockBean
    private LoginLogService loginLogService;
    @MockBean
    private SocialUserService socialUserService;
    @MockBean
    private SmsCodeApi smsCodeApi;
    @MockBean
    private OAuth2TokenService oauth2TokenService;
    @MockBean
    private MemberService memberService;
    @MockBean
    private Validator validator;

    @BeforeEach
    public void setUp() {
        authService.setCaptchaEnable(true);
        // 注入一个 Validator 对象
        ReflectUtil.setFieldValue(authService, "validator",
                Validation.buildDefaultValidatorFactory().getValidator());
    }
}
```

**关键点**：

- `@Import(AdminAuthServiceImpl.class)`：显式导入被测类
- 所有依赖都用 `@MockBean`：隔离被测类，只测试 `AdminAuthServiceImpl` 自身的逻辑
- `setUp()` 中通过 `@Setter` 关闭/开启验证码，通过反射注入真实的 `Validator`

#### 3.2 测试 authenticate() 成功场景

```java
@Test
public void testAuthenticate_success() {
    // 准备参数
    String username = randomString();
    String password = randomString();
    // mock user 数据
    AdminUserDO user = randomPojo(AdminUserDO.class, o -> o.setUsername(username)
            .setPassword(password).setStatus(CommonStatusEnum.ENABLE.getStatus()));
    when(userService.getUserByUsername(eq(username))).thenReturn(user);
    // mock password 匹配
    when(userService.isPasswordMatch(eq(password), eq(user.getPassword()))).thenReturn(true);

    // 调用
    AdminUserDO loginUser = authService.authenticate(username, password);
    // 校验
    assertPojoEquals(user, loginUser);
}
```

**测试模式**：准备参数 → mock 依赖 → 调用被测方法 → 断言结果。

#### 3.3 测试 authenticate() 用户不存在

```java
@Test
public void testAuthenticate_userNotFound() {
    // 准备参数
    String username = randomString();
    String password = randomString();

    // 调用, 并断言异常
    assertServiceException(() -> authService.authenticate(username, password),
            AUTH_LOGIN_BAD_CREDENTIALS);
    verify(loginLogService).createLoginLog(
            argThat(o -> o.getLogType().equals(LoginLogTypeEnum.LOGIN_USERNAME.getType())
                    && o.getResult().equals(LoginResultEnum.BAD_CREDENTIALS.getResult())
                    && o.getUserId() == null)
    );
}
```

**断言要点**：
- 断言抛出了 `AUTH_LOGIN_BAD_CREDENTIALS` 异常
- 断言记录了登录失败日志，且 `userId` 为 null（因为用户不存在）

#### 3.4 测试 login() 完整流程

```java
@Test
public void testLogin_success() {
    // 准备参数
    AuthLoginReqVO reqVO = randomPojo(AuthLoginReqVO.class, o ->
            o.setUsername("test_username").setPassword("test_password")
                    .setSocialType(randomEle(SocialTypeEnum.values()).getType()));

    // mock 验证码正确
    authService.setCaptchaEnable(false);
    // mock user 数据
    AdminUserDO user = randomPojo(AdminUserDO.class, o -> o.setId(1L).setUsername("test_username")
            .setPassword("test_password").setStatus(CommonStatusEnum.ENABLE.getStatus()));
    when(userService.getUserByUsername(eq("test_username"))).thenReturn(user);
    // mock password 匹配
    when(userService.isPasswordMatch(eq("test_password"), eq(user.getPassword()))).thenReturn(true);
    // mock 缓存登录用户到 Redis
    OAuth2AccessTokenDO accessTokenDO = randomPojo(OAuth2AccessTokenDO.class, o -> o.setUserId(1L)
            .setUserType(UserTypeEnum.ADMIN.getValue()));
    when(oauth2TokenService.createAccessToken(eq(1L), eq(UserTypeEnum.ADMIN.getValue()),
            eq("default"), isNull())).thenReturn(accessTokenDO);

    // 调用，并校验
    AuthLoginRespVO loginRespVO = authService.login(reqVO);
    assertPojoEquals(accessTokenDO, loginRespVO);
    // 校验调用参数
    verify(loginLogService).createLoginLog(
            argThat(o -> o.getLogType().equals(LoginLogTypeEnum.LOGIN_USERNAME.getType())
                    && o.getResult().equals(LoginResultEnum.SUCCESS.getResult())
                    && o.getUserId().equals(user.getId()))
    );
    verify(socialUserService).bindSocialUser(eq(new SocialUserBindReqDTO(
            user.getId(), UserTypeEnum.ADMIN.getValue(),
            reqVO.getSocialType(), reqVO.getSocialCode(), reqVO.getSocialState())));
}
```

**这个测试验证了 login() 的完整流程**：
1. 关闭验证码（简化测试）
2. mock 用户查询和密码校验
3. mock 令牌创建
4. 调用 login()
5. 断言返回的令牌数据正确
6. 断言记录了登录成功日志
7. 断言执行了社交用户绑定（因为 socialType 非空）

#### 3.5 测试登出

```java
@Test
public void testLogout_success() {
    // 准备参数
    String token = randomString();
    // mock
    OAuth2AccessTokenDO accessTokenDO = randomPojo(OAuth2AccessTokenDO.class, o -> o.setUserId(1L)
            .setUserType(UserTypeEnum.ADMIN.getValue()));
    when(oauth2TokenService.removeAccessToken(eq(token))).thenReturn(accessTokenDO);

    // 调用
    authService.logout(token, LoginLogTypeEnum.LOGOUT_SELF.getType());
    // 校验调用参数
    verify(loginLogService).createLoginLog(
            argThat(o -> o.getLogType().equals(LoginLogTypeEnum.LOGOUT_SELF.getType())
                    && o.getResult().equals(LoginResultEnum.SUCCESS.getResult()))
    );
}

@Test
public void testLogout_fail() {
    // 准备参数
    String token = randomString();

    // 调用
    authService.logout(token, LoginLogTypeEnum.LOGOUT_SELF.getType());
    // 校验调用参数
    verify(loginLogService, never()).createLoginLog(any());
}
```

**两个场景**：
- 成功登出：令牌存在，删除后记录登出日志
- 令牌不存在：静默返回，不记录日志

### 4. OAuth2TokenServiceImplTest 测试示例

> 源码路径：`yudao-module-system/yudao-module-system-server/src/test/java/cn/iocoder/yudao/module/system/service/oauth2/OAuth2TokenServiceImplTest.java`

#### 4.1 测试类结构

```java
@Import({OAuth2TokenServiceImpl.class, OAuth2AccessTokenRedisDAO.class})
public class OAuth2TokenServiceImplTest extends BaseDbAndRedisUnitTest {

    @Resource
    private OAuth2TokenServiceImpl oauth2TokenService;

    @Resource
    private OAuth2AccessTokenMapper oauth2AccessTokenMapper;
    @Resource
    private OAuth2RefreshTokenMapper oauth2RefreshTokenMapper;
    @Resource
    private OAuth2AccessTokenRedisDAO oauth2AccessTokenRedisDAO;

    @MockBean
    private OAuth2ClientService oauth2ClientService;
    @MockBean
    private AdminUserService adminUserService;
}
```

**关键区别**：继承 `BaseDbAndRedisUnitTest`，因为需要测试真实的 Redis 读写。`OAuth2AccessTokenRedisDAO` 不用 Mock，而是注入真实的 DAO。

#### 4.2 测试 createAccessToken()

```java
@Test
public void testCreateAccessToken() {
    TenantContextHolder.setTenantId(0L);
    // 准备参数
    Long userId = randomLongId();
    Integer userType = UserTypeEnum.ADMIN.getValue();
    String clientId = randomString();
    List<String> scopes = Lists.newArrayList("read", "write");
    // mock 方法
    OAuth2ClientDO clientDO = randomPojo(OAuth2ClientDO.class).setClientId(clientId)
            .setAccessTokenValiditySeconds(30).setRefreshTokenValiditySeconds(60);
    when(oauth2ClientService.validOAuthClientFromCache(eq(clientId))).thenReturn(clientDO);
    // mock 数据（用户）
    AdminUserDO user = randomPojo(AdminUserDO.class);
    when(adminUserService.getUser(userId)).thenReturn(user);

    // 调用
    OAuth2AccessTokenDO accessTokenDO = oauth2TokenService.createAccessToken(
            userId, userType, clientId, scopes);
    // 断言访问令牌
    OAuth2AccessTokenDO dbAccessTokenDO = oauth2AccessTokenMapper
            .selectByAccessToken(accessTokenDO.getAccessToken());
    assertPojoEquals(accessTokenDO, dbAccessTokenDO, "expiresTime", "createTime", "updateTime", "deleted");
    assertEquals(userId, accessTokenDO.getUserId());
    assertEquals(userType, accessTokenDO.getUserType());
    assertEquals(2, accessTokenDO.getUserInfo().size());
    assertEquals(user.getNickname(), accessTokenDO.getUserInfo().get("nickname"));
    assertEquals(user.getDeptId().toString(), accessTokenDO.getUserInfo().get("deptId"));
    assertEquals(clientId, accessTokenDO.getClientId());
    assertEquals(scopes, accessTokenDO.getScopes());
    assertFalse(DateUtils.isExpired(accessTokenDO.getExpiresTime()));
    // 断言访问令牌的缓存
    OAuth2AccessTokenDO redisAccessTokenDO = oauth2AccessTokenRedisDAO
            .get(accessTokenDO.getAccessToken());
    assertPojoEquals(accessTokenDO, redisAccessTokenDO, "expiresTime", "createTime", "updateTime", "deleted");
    // 断言刷新令牌
    OAuth2RefreshTokenDO refreshTokenDO = oauth2RefreshTokenMapper.selectList().get(0);
    assertPojoEquals(accessTokenDO, refreshTokenDO, "id", "expiresTime", "createTime", "updateTime", "deleted");
    assertFalse(DateUtils.isExpired(refreshTokenDO.getExpiresTime()));
}
```

**这个测试验证了**：
1. 访问令牌写入 MySQL（`oauth2AccessTokenMapper.selectByAccessToken` 查到）
2. 访问令牌写入 Redis（`oauth2AccessTokenRedisDAO.get` 查到）
3. 刷新令牌写入 MySQL（`oauth2RefreshTokenMapper.selectList` 查到）
4. 令牌中的 userId、userType、clientId、scopes、userInfo 都正确
5. 令牌未过期

#### 4.3 测试 refreshAccessToken() 令牌轮换

```java
@Test
public void testRefreshAccessToken_success() {
    TenantContextHolder.setTenantId(0L);
    // 准备参数
    String refreshToken = randomString();
    String clientId = randomString();
    // mock 方法
    OAuth2ClientDO clientDO = randomPojo(OAuth2ClientDO.class).setClientId(clientId)
            .setAccessTokenValiditySeconds(30);
    when(oauth2ClientService.validOAuthClientFromCache(eq(clientId))).thenReturn(clientDO);
    // mock 数据（刷新令牌）
    OAuth2RefreshTokenDO refreshTokenDO = randomPojo(OAuth2RefreshTokenDO.class, o ->
            o.setRefreshToken(refreshToken).setClientId(clientId)
                    .setExpiresTime(LocalDateTime.now().plusDays(1))
                    .setUserType(UserTypeEnum.ADMIN.getValue())
                    .setTenantId(TenantContextHolder.getTenantId()));
    oauth2RefreshTokenMapper.insert(refreshTokenDO);
    // mock 数据（旧的访问令牌）
    OAuth2AccessTokenDO accessTokenDO = randomPojo(OAuth2AccessTokenDO.class)
            .setRefreshToken(refreshToken).setUserType(refreshTokenDO.getUserType());
    oauth2AccessTokenMapper.insert(accessTokenDO);
    oauth2AccessTokenRedisDAO.set(accessTokenDO);
    // mock 数据（用户）
    AdminUserDO user = randomPojo(AdminUserDO.class);
    when(adminUserService.getUser(refreshTokenDO.getUserId())).thenReturn(user);

    // 调用
    OAuth2AccessTokenDO newAccessTokenDO = oauth2TokenService.refreshAccessToken(refreshToken, clientId);
    // 断言，老的访问令牌被删除
    assertNull(oauth2AccessTokenMapper.selectByAccessToken(accessTokenDO.getAccessToken()));
    assertNull(oauth2AccessTokenRedisDAO.get(accessTokenDO.getAccessToken()));
    // 断言，新的访问令牌
    OAuth2AccessTokenDO dbAccessTokenDO = oauth2AccessTokenMapper
            .selectByAccessToken(newAccessTokenDO.getAccessToken());
    assertPojoEquals(newAccessTokenDO, dbAccessTokenDO, "expiresTime", "createTime", "updateTime", "deleted");
    assertFalse(DateUtils.isExpired(newAccessTokenDO.getExpiresTime()));
    // 断言，新的访问令牌的缓存
    OAuth2AccessTokenDO redisAccessTokenDO = oauth2AccessTokenRedisDAO
            .get(newAccessTokenDO.getAccessToken());
    assertPojoEquals(newAccessTokenDO, redisAccessTokenDO, "expiresTime", "createTime", "updateTime", "deleted");
}
```

**这个测试验证了令牌轮换**：
1. 旧的访问令牌从 MySQL 和 Redis 中删除
2. 新的访问令牌写入 MySQL 和 Redis
3. 新令牌数据正确

#### 4.4 测试异常场景

```java
@Test
public void testRefreshAccessToken_null() {
    // 调用，并断言
    assertServiceException(() -> oauth2TokenService.refreshAccessToken(randomString(), randomString()),
            new ErrorCode(400, "无效的刷新令牌"));
}

@Test
public void testRefreshAccessToken_clientIdError() {
    // ... mock 数据 ...
    assertServiceException(() -> oauth2TokenService.refreshAccessToken(refreshToken, clientId),
            new ErrorCode(400, "刷新令牌的客户端编号不正确"));
}

@Test
public void testRefreshAccessToken_expired() {
    // ... mock 数据（刷新令牌已过期）...
    assertServiceException(() -> oauth2TokenService.refreshAccessToken(refreshToken, clientId),
            new ErrorCode(401, "刷新令牌已过期"));
    assertEquals(0, oauth2AccessTokenMapper.selectCount()); // 旧令牌也被清理了
}
```

### 5. 编写新测试的建议

#### 5.1 测试 AdminAuthServiceImpl 的模式

```java
// 1. 继承 BaseDbUnitTest
@Import(AdminAuthServiceImpl.class)
public class YourTest extends BaseDbUnitTest {

    // 2. 注入被测类
    @Resource
    private AdminAuthServiceImpl authService;

    // 3. Mock 所有依赖
    @MockBean
    private AdminUserService userService;
    // ... 其他 MockBean ...

    // 4. setUp 中配置验证码开关
    @BeforeEach
    public void setUp() {
        authService.setCaptchaEnable(false); // 测试时通常关闭验证码
    }

    @Test
    public void testXxx() {
        // 5. 准备参数
        // 6. mock 依赖行为
        // 7. 调用被测方法
        // 8. 断言结果 + verify 调用
    }
}
```

#### 5.2 测试 OAuth2TokenServiceImpl 的模式

```java
// 1. 继承 BaseDbAndRedisUnitTest
@Import({OAuth2TokenServiceImpl.class, OAuth2AccessTokenRedisDAO.class})
public class YourTest extends BaseDbAndRedisUnitTest {

    // 2. 注入被测类和 DAO（不用 Mock）
    @Resource
    private OAuth2TokenServiceImpl oauth2TokenService;
    @Resource
    private OAuth2AccessTokenMapper oauth2AccessTokenMapper;
    @Resource
    private OAuth2RefreshTokenMapper oauth2RefreshTokenMapper;
    @Resource
    private OAuth2AccessTokenRedisDAO oauth2AccessTokenRedisDAO;

    // 3. Mock 外部服务
    @MockBean
    private OAuth2ClientService oauth2ClientService;
    @MockBean
    private AdminUserService adminUserService;

    @Test
    public void testXxx() {
        // 4. 设置租户上下文
        TenantContextHolder.setTenantId(0L);
        // 5. 准备参数 + mock
        // 6. 调用
        // 7. 断言 MySQL 数据
        // 8. 断言 Redis 数据
    }
}
```

#### 5.3 常用测试工具方法

| 方法 | 来源 | 用途 |
|------|------|------|
| `randomPojo(Class, Consumer)` | `RandomUtils` | 生成随机 POJO 对象 |
| `randomString()` | `RandomUtils` | 生成随机字符串 |
| `randomLongId()` | `RandomUtils` | 生成随机 Long ID |
| `assertPojoEquals(expected, actual, ignoreFields...)` | `AssertUtils` | 断言两个 POJO 相等（可忽略字段） |
| `assertServiceException(Runnable, ErrorCode)` | `AssertUtils` | 断言抛出了指定的 ServiceException |
| `randomEle(array)` | Hutool `RandomUtil` | 从数组中随机选一个元素 |

---

## 第二部分：源码索引

### 1. 源码文件列表

#### 1.1 实体层（DAL）

| 文件 | 路径 | 说明 |
|------|------|------|
| AdminUserDO ★ | `yudao-module-system-server/.../dal/dataobject/user/AdminUserDO.java` | 用户实体 |
| OAuth2AccessTokenDO ★ | `yudao-module-system-server/.../dal/dataobject/oauth2/OAuth2AccessTokenDO.java` | 访问令牌实体 |
| OAuth2RefreshTokenDO ★ | `yudao-module-system-server/.../dal/dataobject/oauth2/OAuth2RefreshTokenDO.java` | 刷新令牌实体 |
| OAuth2AccessTokenRedisDAO ★ | `yudao-module-system-server/.../dal/redis/oauth2/OAuth2AccessTokenRedisDAO.java` | 访问令牌 Redis DAO |

#### 1.2 服务层（Service）

| 文件 | 路径 | 说明 |
|------|------|------|
| AdminAuthService ★ | `yudao-module-system-server/.../service/auth/AdminAuthService.java` | 认证服务接口 |
| AdminAuthServiceImpl ★ | `yudao-module-system-server/.../service/auth/AdminAuthServiceImpl.java` | 认证服务实现 |
| OAuth2TokenService ★ | `yudao-module-system-server/.../service/oauth2/OAuth2TokenService.java` | 令牌服务接口 |
| OAuth2TokenServiceImpl ★ | `yudao-module-system-server/.../service/oauth2/OAuth2TokenServiceImpl.java` | 令牌服务实现 |

#### 1.3 控制器层（Controller）

| 文件 | 路径 | 说明 |
|------|------|------|
| AuthController ★ | `yudao-module-system-server/.../controller/admin/auth/AuthController.java` | 认证端点 |

#### 1.4 VO 层

| 文件 | 路径 | 说明 |
|------|------|------|
| CaptchaVerificationReqVO | `.../controller/admin/auth/vo/CaptchaVerificationReqVO.java` | 验证码基类 |
| AuthLoginReqVO ★ | `.../controller/admin/auth/vo/AuthLoginReqVO.java` | 账号密码登录请求 |
| AuthLoginRespVO ★ | `.../controller/admin/auth/vo/AuthLoginRespVO.java` | 登录成功响应 |
| AuthSmsLoginReqVO | `.../controller/admin/auth/vo/AuthSmsLoginReqVO.java` | 短信登录请求 |
| AuthSmsSendReqVO | `.../controller/admin/auth/vo/AuthSmsSendReqVO.java` | 发送短信请求 |
| AuthSocialLoginReqVO | `.../controller/admin/auth/vo/AuthSocialLoginReqVO.java` | 社交登录请求 |
| AuthRegisterReqVO | `.../controller/admin/auth/vo/AuthRegisterReqVO.java` | 注册请求 |
| AuthResetPasswordReqVO | `.../controller/admin/auth/vo/AuthResetPasswordReqVO.java` | 重置密码请求 |
| AuthPermissionInfoRespVO ★ | `.../controller/admin/auth/vo/AuthPermissionInfoRespVO.java` | 权限信息响应 |
| AuthMenuRespVO | `.../controller/admin/auth/vo/AuthMenuRespVO.java` | 菜单响应 |

#### 1.5 转换层（Convert）

| 文件 | 路径 | 说明 |
|------|------|------|
| AuthConvert ★ | `yudao-module-system-server/.../convert/auth/AuthConvert.java` | VO/DTO 转换器 |

#### 1.6 测试

| 文件 | 路径 | 说明 |
|------|------|------|
| AdminAuthServiceImplTest ★ | `yudao-module-system-server/src/test/.../service/auth/AdminAuthServiceImplTest.java` | 认证服务测试 |
| OAuth2TokenServiceImplTest ★ | `yudao-module-system-server/src/test/.../service/oauth2/OAuth2TokenServiceImplTest.java` | 令牌服务测试 |

（★ 标记为核心文件）

### 2. 业务层使用示例

认证功能主要由前端直接调用，业务代码中较少直接使用。典型的间接使用场景：

```java
// 在其他业务 Service 中获取当前登录用户
Long userId = SecurityFrameworkUtils.getLoginUserId();

// 管理员禁用用户时，强制踢掉在线会话
oauth2TokenService.removeAccessToken(userId, UserTypeEnum.ADMIN.getValue());
```

### 3. 配置文件

```yaml
# application.yml 中的相关配置
yudao:
  captcha:
    enable: true                    # 是否开启验证码
  security:
    token-header: Authorization     # Token 请求头
    token-parameter: access_token   # Token 请求参数（备选）

# OAuth2 客户端配置（数据库 system_oauth2_client 表）
# client_id: default
# access_token_validity_seconds: 7200    (2小时)
# refresh_token_validity_seconds: 604800 (7天)
```

### 4. 章节与源码对照表

| 章节 | 核心源码 | 关键行号 |
|------|---------|---------|
| 01-用户实体与VO | AdminUserDO.java | 全文（96行） |
| 01-用户实体与VO | AuthLoginReqVO.java | 全文（57行） |
| 01-用户实体与VO | AuthLoginRespVO.java | 全文（30行） |
| 01-用户实体与VO | AuthPermissionInfoRespVO.java | 全文（102行） |
| 02-AdminAuthServiceImpl | AdminAuthServiceImpl.java | L82-100 authenticate() |
| 02-AdminAuthServiceImpl | AdminAuthServiceImpl.java | L103-118 login() |
| 02-AdminAuthServiceImpl | AdminAuthServiceImpl.java | L139-151 smsLogin() |
| 02-AdminAuthServiceImpl | AdminAuthServiceImpl.java | L173-189 socialLogin() |
| 02-AdminAuthServiceImpl | AdminAuthServiceImpl.java | L213-221 createTokenAfterLoginSuccess() |
| 02-AdminAuthServiceImpl | AdminAuthServiceImpl.java | L224-227 refreshToken() |
| 02-AdminAuthServiceImpl | AdminAuthServiceImpl.java | L230-238 logout() |
| 03-OAuth2TokenServiceImpl | OAuth2TokenServiceImpl.java | L63-69 createAccessToken() |
| 03-OAuth2TokenServiceImpl | OAuth2TokenServiceImpl.java | L73-101 refreshAccessToken() |
| 03-OAuth2TokenServiceImpl | OAuth2TokenServiceImpl.java | L104-128 getAccessToken() |
| 03-OAuth2TokenServiceImpl | OAuth2TokenServiceImpl.java | L131-140 checkAccessToken() |
| 03-OAuth2TokenServiceImpl | OAuth2TokenServiceImpl.java | L143-156 removeAccessToken(token) |
| 03-OAuth2TokenServiceImpl | OAuth2TokenServiceImpl.java | L179-197 createOAuth2AccessToken() |
| 03-OAuth2TokenServiceImpl | OAuth2TokenServiceImpl.java | L238-244 generateAccessToken/RefreshToken() |
| 03-OAuth2TokenServiceImpl | OAuth2AccessTokenRedisDAO.java | L30-43 get/set |
| 04-AuthController | AuthController.java | L66-71 login 端点 |
| 04-AuthController | AuthController.java | L93-118 getPermissionInfo 端点 |
| 04-AuthController | AuthController.java | L73-83 logout 端点 |
| 04-AuthController | AuthController.java | L85-91 refreshToken 端点 |
| 05-测试支持 | AdminAuthServiceImplTest.java | 全文（360行） |
| 05-测试支持 | OAuth2TokenServiceImplTest.java | 全文（330行） |

### 5. 数据库表

| 表名 | 说明 | 关联实体 |
|------|------|---------|
| system_users | 管理后台用户表 | AdminUserDO |
| system_oauth2_access_token | OAuth2 访问令牌表 | OAuth2AccessTokenDO |
| system_oauth2_refresh_token | OAuth2 刷新令牌表 | OAuth2RefreshTokenDO |
| system_oauth2_client | OAuth2 客户端配置表 | OAuth2ClientDO |
| system_user_role | 用户角色关联表 | - |
| system_role_menu | 角色菜单关联表 | - |
| system_login_log | 登录日志表 | - |
| system_social_user | 社交用户绑定表 | - |

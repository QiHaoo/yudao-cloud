# 源码索引：声明式 RPC 调用

## 源码文件列表

### 公共层（yudao-common）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ RpcConstants | `framework/common/enums/RpcConstants.java` | RPC 常量：RPC_API_PREFIX = "/rpc-api"，各服务名和前缀 |

### 框架层 - 安全模块（yudao-spring-boot-starter-security）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ LoginUserRequestInterceptor | `framework/security/core/rpc/LoginUserRequestInterceptor.java` | Feign 拦截器，传递 LoginUser 到 Header |
| YudaoSecurityRpcAutoConfiguration | `framework/security/config/YudaoSecurityRpcAutoConfiguration.java` | RPC 安全自动配置 |

### 框架层 - 数据权限模块（yudao-spring-boot-starter-biz-data-permission）

| 文件 | 路径 | 说明 |
|------|------|------|
| DataPermissionRequestInterceptor | `framework/datapermission/core/rpc/DataPermissionRequestInterceptor.java` | Feign 拦截器，传递数据权限 enable=false 到 Header |

### 框架层 - 多租户模块（yudao-spring-boot-starter-biz-tenant）

| 文件 | 路径 | 说明 |
|------|------|------|
| TenantRequestInterceptor | 框架层租户模块 | Feign 拦截器，传递 tenant-id 到 Header |

### API 模块示例（yudao-module-system-api）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ ApiConstants | `module/system/enums/ApiConstants.java` | 服务名（system-server）和前缀（/rpc-api/system） |
| ★ AdminUserApi | `module/system/api/user/AdminUserApi.java` | 用户 Feign 接口，定义 getUser / getUserList |
| AdminUserRespDTO | `module/system/api/user/dto/AdminUserRespDTO.java` | 用户响应 DTO |
| ★ PermissionApi | `module/system/api/permission/PermissionApi.java` | 权限 Feign 接口 |
| DeptApi | `module/system/api/dept/DeptApi.java` | 部门 Feign 接口 |
| ErrorCodeConstants | `module/system/enums/ErrorCodeConstants.java` | 错误码常量 |

### API 模块示例（yudao-module-infra-api）

| 文件 | 路径 | 说明 |
|------|------|------|
| ApiConstants | `module/infra/enums/ApiConstants.java` | 服务名（infra-server）和前缀（/rpc-api/infra） |

### 实现层示例（yudao-module-system-server）

| 文件 | 路径 | 说明 |
|------|------|------|
| ★ AdminUserRpcController | `module/system/controller/admin/user/AdminUserRpcController.java` | 用户 RPC 接口实现，implements AdminUserApi |
| PermissionRpcController | 模块内 RPC Controller | 权限 RPC 接口实现 |

### 配置文件

| 文件 | 路径 | 说明 |
|------|------|------|
| pom.xml | `yudao-dependencies/pom.xml` | BOM，管理 spring-cloud-starter-openfeign 版本 |
| application.yaml | 各服务 `src/main/resources/` | 含 spring.cloud.openfeign 配置 |

### 业务层使用示例

| 文件 | 路径 | 说明 |
|------|------|------|
| PermissionServiceImpl | `module/system/service/permission/PermissionServiceImpl.java` | 使用 PermissionCommonApi 进行 RPC 调用 |

## 业务层使用示例

### 定义 Feign 接口

```java
// *-api 模块中的接口定义
@FeignClient(name = ApiConstants.NAME)
@Tag(name = "RPC 服务 - 商品 SPU")
public interface ProductSpuApi {

    String PREFIX = ApiConstants.PREFIX + "/spu";

    @GetMapping(PREFIX + "/get")
    CommonResult<ProductSpuRespDTO> getSpu(@RequestParam("id") Long id);

    @GetMapping(PREFIX + "/list")
    CommonResult<List<ProductSpuRespDTO>> getSpuList(@RequestParam("ids") Collection<Long> ids);
}
```

### 调用方使用

```java
@Service
public class OrderService {

    @Resource
    private ProductSpuApi productSpuApi;

    public void createOrder(Long spuId) {
        // 像调用本地方法一样调用远程服务
        CommonResult<ProductSpuRespDTO> result = productSpuApi.getSpu(spuId);
        ProductSpuRespDTO spu = result.getCheckedData();
        // ...
    }
}
```

### 实现方提供接口

```java
@RestController
@RequestMapping("/rpc-api/product/spu")
public class ProductSpuController implements ProductSpuApi {

    @Resource
    private ProductService productService;

    @Override
    @GetMapping("/get")
    public CommonResult<ProductSpuRespDTO> getSpu(@RequestParam("id") Long id) {
        ProductSpuRespDTO spu = productService.getSpu(id);
        return CommonResult.success(spu);
    }
}
```

## 章节 ↔ 源码对照表

### 功能设计

| 章节 | 核心源码 |
|------|---------|
| 功能设计 01-总览 | OpenFeign 概念、*-api 模块设计 |
| 功能设计 02-问题域 | 传统 RestTemplate 痛点、Dubbo/gRPC 对比 |
| 功能设计 03-核心设计 | ★ @FeignClient、★ RpcConstants、★ *-api 模块结构、RequestInterceptor |

### 功能实现

| 章节 | 核心源码 |
|------|---------|
| 功能实现 01-Feign客户端 | ★ *-api 模块 pom.xml、★ ApiConstants、★ ProductSpuApi、ProductSpuRespDTO |
| 功能实现 02-RPC调用链路 | LoginUserRequestInterceptor、TenantRequestInterceptor、DataPermissionRequestInterceptor |
| 功能实现 03-测试支持 | 本文件 |

### 深度学习

| 章节 | 关联源码 |
|------|---------|
| 深度学习 01-设计模式提炼 | Feign 代理（代理模式）、FeignClientFactoryBean（工厂模式）、RequestInterceptor（拦截器链）、*-api 模块（契约优先） |

## 配置项速查

### OpenFeign 配置

| 配置项 | 作用 | 示例值 |
|--------|------|--------|
| `spring.cloud.openfeign.sentinel.enabled` | 开启 Sentinel 熔断 | `true` |
| `spring.cloud.openfeign.client.config.default.logger-level` | 日志级别 | `BASIC` |
| `logging.level.cn.iocoder.yudao.module.*.api` | Feign 接口日志 | `DEBUG` |

### URL 前缀规范

| 类型 | 前缀 | 说明 |
|------|------|------|
| 管理后台 | `/admin-api/{module}/...` | 管理员操作 |
| 用户端 | `/app-api/{module}/...` | 用户操作 |
| RPC 调用 | `/rpc-api/{module}/...` | 服务间调用 |

### 命名规范

| 元素 | 命名规范 | 示例 |
|------|----------|------|
| API 接口 | `{业务}Api` | `ProductSpuApi` |
| 响应 DTO | `{业务}RespDTO` | `ProductSpuRespDTO` |
| 请求 DTO | `{业务}SaveReqDTO` / `{业务}PageReqDTO` | `ProductSpuSaveReqDTO` |
| API 常量 | `ApiConstants` | `ApiConstants.NAME` |
| 错误码 | `ErrorCodeConstants` | `ErrorCodeConstants.SPU_NOT_EXISTS` |
| RPC Controller | `{业务}RpcController` | `AdminUserRpcController` |
